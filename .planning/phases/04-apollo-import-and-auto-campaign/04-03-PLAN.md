---
phase: 04-apollo-import-and-auto-campaign
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - backend/src/routes/apollo.ts
  - backend/src/seeds/stream-templates.ts
  - backend/src/routes/email-templates.ts
  - backend/src/app.ts
autonomous: true
requirements:
  - REQ-041
  - REQ-042
  - REQ-043
must_haves:
  truths:
    - "POST /api/apollo/import accepts a filters object and returns ApolloImportResponse"
    - "Route returns 503 with hint when APOLLO_API_KEY env var is missing"
    - "Route returns 503 when Apollo returns 401 (key invalid/expired)"
    - "Imported Apollo contacts have source='apollo', stream=<classified>, apolloPersonId=<unique>"
    - "Imported Apollo companies have dataSource='apollo', stream=<classified>, apolloOrgId=<unique>"
    - "Re-running an import for the same Apollo person/org dedupes (skip count climbs, no duplicate Contact row)"
    - "POST /api/email-templates/seed-streams creates 9 EmailTemplate rows (one per stream) for the calling user, idempotently"
    - "GET /api/email-templates?category=Stream:<name> returns the seeded template for that stream"
    - "Response includes contactIds[] and suggestedStream for wizard handoff"
    - "POST /api/apollo/send-campaign accepts contactIds + templateId + suggestedStream and dispatches via Resend"
    - "From-address is hardcoded Sara <sara@techcloudpro.com> in Phase 4"
    - "RESEND_API_KEY env var required at backend boot; missing → process.exit(1) with clear error"
    - "/api/apollo/send-campaign substitutes {{firstName}} and {{companyName}} per contact (mirrors campaigns.ts:546-559 substitution logic)"
    - "/api/apollo/send-campaign returns aggregated { sent, failed, failureDetails } — never fail-fast on per-contact errors"
    - "/api/apollo/send-campaign does NOT import @aws-sdk/client-ses (verifiable via grep)"
  artifacts:
    - path: "backend/src/routes/apollo.ts"
      provides: "POST /api/apollo/import (search→enrich→classify→upsert) + POST /api/apollo/send-campaign (Resend dispatcher)"
      exports: ["default"]
      min_lines: 220
    - path: "backend/src/seeds/stream-templates.ts"
      provides: "Idempotent seeder for the 9 Stream:* EmailTemplate rows (one per stream + Other)"
      exports: ["seedStreamTemplates", "STREAM_TEMPLATE_SEEDS"]
    - path: "backend/src/routes/email-templates.ts"
      provides: "Adds POST /seed-streams admin endpoint + GET ?category= filter"
      contains: "seed-streams"
    - path: "backend/src/app.ts"
      provides: "Mounts apolloRouter at /api/apollo"
      contains: "/api/apollo"
  key_links:
    - from: "backend/src/routes/apollo.ts"
      to: "backend/src/lib/apolloClient.ts"
      via: "import { searchPeople, enrichPerson, isEmailLocked, ApolloAuthError, sleep }"
      pattern: "from.*lib/apolloClient"
    - from: "backend/src/routes/apollo.ts"
      to: "backend/src/lib/streamClassifier.ts"
      via: "import { classifyStream }"
      pattern: "from.*lib/streamClassifier"
    - from: "backend/src/routes/apollo.ts"
      to: "prisma.contact + prisma.company"
      via: "findUnique on apolloPersonId/apolloOrgId then create with stream + source"
      pattern: "prisma\\.contact\\.(findUnique|create)"
    - from: "backend/src/routes/apollo.ts"
      to: "Resend API (https://api.resend.com via SDK)"
      via: "import { Resend } from 'resend' + resend.emails.send"
      pattern: "from 'resend'|resend\\.emails\\.send"
    - from: "backend/src/app.ts"
      to: "backend/src/routes/apollo.ts"
      via: "app.use('/api/apollo', apolloRouter)"
      pattern: "/api/apollo"
---

<objective>
Build the backend orchestration layer that turns the lib primitives from plans 04-01 (schema + classifier)
and 04-02 (Apollo HTTP client) into a working `POST /api/apollo/import` endpoint, plus the
EmailTemplate seed endpoint that pre-creates the 9 stream-specific templates the wizard in plan 04-04
will consume, plus the NEW `POST /api/apollo/send-campaign` Resend dispatcher that the wizard's Step 4
will call directly (bypassing the existing campaigns.ts SES path so Rajesh's BrandMonkz flow stays
untouched).

Purpose: REQ-041 + REQ-042 + REQ-043 — make Apollo→DB+classification AND Apollo→Resend send work
end-to-end behind two HTTP calls in the SAME route file.
Output: One new route file with TWO endpoints, one new seed file, one route file modification, app.ts mount.

Send path note (USER-LOCKED 2026-05-30): Phase 4 does NOT modify campaigns.ts. The wizard sends through a
new `/api/apollo/send-campaign` route that uses the `resend` npm SDK (already in dependencies — `package.json`
declares `"resend": "^6.4.0"`). From-address hardcoded to Sara <sara@techcloudpro.com> per memory
`brandmonkz_two_divergent_repos` (techcloudpro.com domain verified in Resend, May 26 swap from Peter → Sara).
</objective>

<execution_context>
@/Users/jeet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jeet/production-crm/.planning/phases/04-apollo-import-and-auto-campaign/04-RESEARCH.md
@/Users/jeet/production-crm/.planning/phases/04-apollo-import-and-auto-campaign/04-01-SUMMARY.md
@/Users/jeet/production-crm/.planning/phases/04-apollo-import-and-auto-campaign/04-02-SUMMARY.md

Source files to read before editing:
@/Users/jeet/production-crm/backend/src/app.ts
@/Users/jeet/production-crm/backend/src/routes/job-leads.routes.ts
@/Users/jeet/production-crm/backend/src/routes/enrichment.ts
@/Users/jeet/production-crm/backend/src/routes/email-templates.ts
@/Users/jeet/production-crm/backend/src/middleware/auth.ts
@/Users/jeet/production-crm/backend/src/routes/campaigns.ts  # READ ONLY for substitution-logic reference (lines 546-559); DO NOT MODIFY
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend/src/routes/apollo.ts (POST /api/apollo/import) and mount in app.ts</name>
  <files>
    backend/src/routes/apollo.ts
    backend/src/app.ts
  </files>
  <action>
Create `backend/src/routes/apollo.ts`. Follow the existing route-file conventions from `job-leads.routes.ts` (auth middleware, error shape, prisma usage). Skeleton:

```typescript
// backend/src/routes/apollo.ts
import { Router, Request, Response } from 'express';
import { PrismaClient } from '@prisma/client';
import { authenticate } from '../middleware/auth';  // or whatever the actual import is — verify against job-leads.routes.ts
import {
  searchPeople,
  enrichPerson,
  isEmailLocked,
  ApolloAuthError,
  sleep,
  ApolloPerson,
  ApolloSearchFilters,
} from '../lib/apolloClient';
import { classifyStream } from '../lib/streamClassifier';

const router = Router();
const prisma = new PrismaClient();

router.use(authenticate);  // mirror job-leads.routes.ts pattern

interface ApolloImportRequestBody {
  filters: ApolloSearchFilters;
  enrich?: boolean;
}

interface ApolloImportResponse {
  imported: number;
  skipped: number;
  total: number;
  contactIds: string[];
  suggestedStream: string;
  errors: Array<{ apolloPersonId: string; reason: string }>;
}

router.post('/import', async (req: Request, res: Response) => {
  // 1. Pre-check env
  const apiKey = process.env.APOLLO_API_KEY;
  if (!apiKey) {
    return res.status(503).json({
      error: 'Apollo not configured',
      hint: 'Set APOLLO_API_KEY in backend/.env (live key lives on EC2 — SCP from /var/www/crm-backend/.env)',
    });
  }

  const { filters, enrich = true } = (req.body || {}) as ApolloImportRequestBody;
  if (!filters || typeof filters !== 'object') {
    return res.status(400).json({ error: 'filters object required' });
  }

  try {
    // 2. Search
    const searchResp = await searchPeople(apiKey, filters);
    const people = searchResp.people || [];

    // 3. Enrich + upsert loop
    const contactIds: string[] = [];
    const errors: ApolloImportResponse['errors'] = [];
    const streamCounts: Record<string, number> = {};
    let skipped = 0;

    for (const baseP of people) {
      let person: ApolloPerson = baseP;
      if (enrich && isEmailLocked(baseP.email)) {
        await sleep(1500);  // pace per Pitfall 7 (Apollo rate-limits ~30/min)
        const enriched = await enrichPerson(apiKey, baseP.id);
        if (enriched) person = { ...baseP, ...enriched };
      }

      if (isEmailLocked(person.email)) {
        errors.push({ apolloPersonId: person.id, reason: 'email locked after enrich' });
        continue;
      }

      // Dedup: skip if apolloPersonId already imported
      const existing = await prisma.contact.findUnique({ where: { apolloPersonId: person.id } });
      if (existing) {
        skipped++;
        errors.push({ apolloPersonId: person.id, reason: 'already imported' });
        continue;
      }

      // Classify: concat title + industry + keywords
      const org = person.organization || {};
      const classifierText = [
        org.industry || '',
        ...(org.keywords || []),
      ].join(' ');
      const stream = classifyStream(person.title || '', classifierText);
      streamCounts[stream] = (streamCounts[stream] || 0) + 1;

      // Upsert Company by apolloOrgId
      let companyId: string | null = null;
      if (org.id) {
        const existingCo = await prisma.company.findUnique({ where: { apolloOrgId: org.id } });
        if (existingCo) {
          companyId = existingCo.id;
        } else if (org.name) {
          const created = await prisma.company.create({
            data: {
              name: org.name,
              website: org.website_url || null,
              industry: org.industry || null,
              employeeCount: org.estimated_num_employees ?? null,  // adapt field name to actual schema
              stream,
              apolloOrgId: org.id,
              apolloRawData: org as any,
              dataSource: 'apollo',
              userId: (req as any).user?.id || (req as any).user?.sub,  // verify the actual user-id key vs job-leads route
            },
          });
          companyId = created.id;
        }
      }

      // Create Contact
      const newContact = await prisma.contact.create({
        data: {
          firstName: person.first_name || null,
          lastName: person.last_name || null,
          email: person.email!,
          jobTitle: person.title || null,
          linkedinUrl: person.linkedin_url || null,
          source: 'apollo',
          stream,
          apolloPersonId: person.id,
          apolloRawData: person as any,
          companyId,
          userId: (req as any).user?.id || (req as any).user?.sub,
        },
      });
      contactIds.push(newContact.id);
    }

    // 4. Derive suggestedStream = most common
    const suggestedStream = Object.entries(streamCounts).sort((a, b) => b[1] - a[1])[0]?.[0] || 'Other';

    const body: ApolloImportResponse = {
      imported: contactIds.length,
      skipped,
      total: people.length,
      contactIds,
      suggestedStream,
      errors,
    };
    return res.status(200).json(body);
  } catch (err: any) {
    if (err instanceof ApolloAuthError) {
      return res.status(503).json({ error: 'Apollo key invalid or expired' });
    }
    console.error('[apollo.import] unexpected error', err);
    return res.status(500).json({ error: 'Apollo import failed', detail: err?.message });
  }
});

export default router;
```

CRITICAL ADAPTATIONS — read these files first and match the actual field/middleware names:
- `backend/src/middleware/auth.ts` → confirm the export name (might be `authenticate`, `requireAuth`, etc.)
- `backend/src/routes/job-leads.routes.ts:462-503` → see the actual Company/Contact create shape (field names like `employeeCount` vs `numEmployees`, `linkedinUrl` vs `linkedIn`, etc.) Match these EXACTLY — don't guess.
- `(req as any).user?.id` → confirm the actual user-id property by checking how `job-leads.routes.ts:485` or `enrichment.ts:117` accesses it.

The skeleton above is intent + structure. Field names must come from the actual schema.

Then edit `backend/src/app.ts`:
- Add import near other route imports: `import apolloRouter from './routes/apollo';`
- Add mount near `enrichmentRoutes` (per researcher line 33): `app.use('/api/apollo', apolloRouter);`
- Place it close to other `/api/...` route mounts; preserve alphabetical ordering if the file uses that convention.

NOTE: Task 3 below will ADD a second route handler to this same `apollo.ts` file (POST /send-campaign). Structure the file with both `import { Resend }` at top and `/import` handler first, leaving room for the second handler. Plan for both endpoints in this same file from the start — do NOT split into two route files.
  </action>
  <verify>
```bash
ls /Users/jeet/production-crm/backend/src/routes/apollo.ts
grep -n "router.post.*'/import'" /Users/jeet/production-crm/backend/src/routes/apollo.ts
grep -n "from '../lib/apolloClient'" /Users/jeet/production-crm/backend/src/routes/apollo.ts
grep -n "from '../lib/streamClassifier'" /Users/jeet/production-crm/backend/src/routes/apollo.ts
grep -n "ApolloAuthError" /Users/jeet/production-crm/backend/src/routes/apollo.ts
grep -n "'/api/apollo'" /Users/jeet/production-crm/backend/src/app.ts
cd /Users/jeet/production-crm/backend && npx tsc --noEmit 2>&1 | grep -E "apollo|app\.ts" | head -20
```
All greps return matches; tsc clean for apollo.ts and app.ts.

503 dry-run (without the env var set):
```bash
cd /Users/jeet/production-crm/backend && unset APOLLO_API_KEY && npm run dev &
sleep 5
curl -sS -X POST http://localhost:5000/api/apollo/import -H 'Authorization: Bearer <dev-jwt>' -H 'Content-Type: application/json' -d '{"filters":{"perPage":1}}' -o /tmp/apollo-503.json -w '%{http_code}\n'
cat /tmp/apollo-503.json
kill %1
```
Expected: HTTP 503, body contains "Apollo not configured".
  </verify>
  <done>
apollo.ts route exists, mounted at /api/apollo in app.ts, returns 503 when key missing, classifies + dedupes + upserts on real Apollo response. tsc clean. Local 503-without-key smoke passes (full happy-path smoke runs in plan 04-06 on EC2 after SCP).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stream-template seeder + GET-by-category + POST seed endpoint</name>
  <files>
    backend/src/seeds/stream-templates.ts
    backend/src/routes/email-templates.ts
  </files>
  <action>
**Step A — create `backend/src/seeds/stream-templates.ts`:**

```typescript
// backend/src/seeds/stream-templates.ts
// Idempotent seeder for stream-specific EmailTemplate rows.
// Called by POST /api/email-templates/seed-streams (one-shot per user) AND
// optionally during user-signup hook in a future phase.
//
// Each row uses EmailTemplate.category = `Stream:<name>` so the wizard in
// plan 04-04 can `findFirst({ where: { category: 'Stream:NetSuite', userId } })`.

import { PrismaClient } from '@prisma/client';

export const STREAM_TEMPLATE_SEEDS = [
  { stream: 'NetSuite',        subject: 'NetSuite implementation help — quick chat?',         htmlSnippet: '<p>Hi {{firstName}},</p><p>We help companies like {{companyName}} cut NetSuite go-live time by 40%. Open to a 15-min call?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'AI/ML',           subject: 'AI/ML engineering capacity for {{companyName}}',     htmlSnippet: '<p>Hi {{firstName}},</p><p>Our AI/ML bench has GPU-trained engineers available immediately. Could we share two profiles for {{companyName}}?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Cloud/DevOps',    subject: 'AWS/Azure architects for {{companyName}}',           htmlSnippet: '<p>Hi {{firstName}},</p><p>Senior cloud architects with multi-region experience are on our bench. Want to see a few profiles for {{companyName}}?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Cybersecurity',   subject: 'Security engineers — bench available',                htmlSnippet: '<p>Hi {{firstName}},</p><p>Cleared SOC analysts + AppSec engineers available. Quick chat for {{companyName}}?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Data/Analytics',  subject: 'Data engineering capacity for {{companyName}}',      htmlSnippet: '<p>Hi {{firstName}},</p><p>Snowflake + dbt engineers available. Want to walk through {{companyName}}\'s pipeline?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Mobile',          subject: 'iOS/Android engineers for {{companyName}}',           htmlSnippet: '<p>Hi {{firstName}},</p><p>Native + cross-platform mobile engineers on bench. 15-min intro for {{companyName}}?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Enterprise/ERP',  subject: 'ERP implementation help — quick chat?',              htmlSnippet: '<p>Hi {{firstName}},</p><p>Cross-platform ERP consultants (SAP / NetSuite / Oracle) for {{companyName}}. Worth a chat?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Staffing/HR',     subject: 'Tech staffing partnership for {{companyName}}',      htmlSnippet: '<p>Hi {{firstName}},</p><p>We supplement in-house recruiting with pre-vetted tech profiles. Worth a 15-min call for {{companyName}}?</p><p>— Sara, TechCloudPro</p>' },
  { stream: 'Other',           subject: 'Tech engineers available for {{companyName}}',       htmlSnippet: '<p>Hi {{firstName}},</p><p>We place senior engineers across the stack. Quick chat about {{companyName}}\'s near-term needs?</p><p>— Sara, TechCloudPro</p>' },
];

export async function seedStreamTemplates(
  prisma: PrismaClient,
  userId: string,
): Promise<{ created: string[]; skipped: string[] }> {
  const created: string[] = [];
  const skipped: string[] = [];
  for (const seed of STREAM_TEMPLATE_SEEDS) {
    const category = `Stream:${seed.stream}`;
    const existing = await prisma.emailTemplate.findFirst({ where: { userId, category } });
    if (existing) {
      skipped.push(seed.stream);
      continue;
    }
    await prisma.emailTemplate.create({
      data: {
        userId,
        name: `Stream: ${seed.stream}`,
        category,
        subject: seed.subject,
        htmlBody: seed.htmlSnippet,
        // Match actual schema — verify other required fields (body, textBody, etc.)
      },
    });
    created.push(seed.stream);
  }
  return { created, skipped };
}
```

IMPORTANT: Read `backend/prisma/schema.prisma` model EmailTemplate first and confirm which fields are REQUIRED (e.g., `body String` vs `htmlBody String?`). Adjust the `data:` object to satisfy all `String` (non-optional) fields. If `textBody` is required, supply a plain-text version.

Sender update: signature is `Sara, TechCloudPro` (NOT `Rajesh, BrandMonkz`) — matches Phase 4 locked decision to use Resend with from-address `Sara <sara@techcloudpro.com>` (memory `brandmonkz_two_divergent_repos`: May 26 Peter→Sara swap, techcloudpro.com is the verified domain).

**Step B — add 2 endpoints to `backend/src/routes/email-templates.ts`:**

Read the file first. Add (a) `POST /seed-streams` and (b) `GET ?category=...` filter support.

```typescript
// At top of file (with existing imports):
import { seedStreamTemplates, STREAM_TEMPLATE_SEEDS } from '../seeds/stream-templates';

// New endpoint — placed near other POST handlers:
router.post('/seed-streams', async (req: Request, res: Response) => {
  const userId = (req as any).user?.id || (req as any).user?.sub;
  if (!userId) return res.status(401).json({ error: 'unauthenticated' });
  const result = await seedStreamTemplates(prisma, userId);
  return res.json({ ...result, total: STREAM_TEMPLATE_SEEDS.length });
});
```

For the GET-by-category filter: locate the existing `GET /` (list) handler. Extend it to honor `?category=` query param:

```typescript
const { category } = req.query;
const where: any = { userId };
if (typeof category === 'string' && category) where.category = category;
const templates = await prisma.emailTemplate.findMany({ where, orderBy: { createdAt: 'desc' } });
return res.json({ templates });
```

If the route file uses a different list-endpoint name (e.g., `/list`), apply the same filter there.

The wizard in plan 04-05 will call `GET /api/email-templates?category=Stream:NetSuite` (or whatever stream) and use the first returned row.
  </action>
  <verify>
```bash
ls /Users/jeet/production-crm/backend/src/seeds/stream-templates.ts
grep -n "export async function seedStreamTemplates" /Users/jeet/production-crm/backend/src/seeds/stream-templates.ts
grep -c "stream:" /Users/jeet/production-crm/backend/src/seeds/stream-templates.ts
# Expected: 9+ (one per stream in STREAM_TEMPLATE_SEEDS)
grep -n "seed-streams" /Users/jeet/production-crm/backend/src/routes/email-templates.ts
grep -n "category" /Users/jeet/production-crm/backend/src/routes/email-templates.ts
cd /Users/jeet/production-crm/backend && npx tsc --noEmit 2>&1 | grep -E "stream-templates|email-templates" | head -20
```
All greps return matches; tsc clean.

Live seed dry-run (against local DB — requires JWT but no Apollo key):
```bash
cd /Users/jeet/production-crm/backend && npm run dev &
sleep 5
TOKEN=$(curl -sS -X POST http://localhost:5000/api/auth/login -H 'Content-Type: application/json' -d '{"email":"<dev-user>","password":"<dev-pass>"}' | jq -r .token)
curl -sS -X POST http://localhost:5000/api/email-templates/seed-streams -H "Authorization: Bearer $TOKEN" | jq
# Expected: { created: ["NetSuite","AI/ML",...], skipped: [], total: 9 }
curl -sS "http://localhost:5000/api/email-templates?category=Stream:NetSuite" -H "Authorization: Bearer $TOKEN" | jq '.templates | length'
# Expected: 1
kill %1
```

Re-run the seed — second call should show 9 entries in `skipped[]` and 0 in `created[]` (idempotency check).
  </verify>
  <done>
Seed file created with 9 stream templates (Sara/TechCloudPro signature); POST /api/email-templates/seed-streams works and is idempotent; GET /api/email-templates?category=Stream:<x> returns the seeded row; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add POST /api/apollo/send-campaign Resend dispatcher to apollo.ts (USER-LOCKED Phase 4 send path)</name>
  <files>
    backend/src/routes/apollo.ts
  </files>
  <action>
USER-LOCKED DECISION (2026-05-30): Phase 4 wizard sends through a NEW endpoint that uses the Resend npm SDK directly. It does NOT modify `campaigns.ts` (Rajesh's existing BrandMonkz SES path stays untouched). It is net-new infrastructure, parallel to `/api/campaigns/:id/send`.

**Prerequisites verified during planning:**
- `resend` npm package is already declared in `package.json` (`"resend": "^6.4.0"`). NO `npm install` needed.
- `RESEND_API_KEY` is the Resend authentication. NOT `SES_FROM_EMAIL`, NOT `AWS_REGION`, NOT `SMTP_USER`.
- From-address is hardcoded to `Sara <sara@techcloudpro.com>` per memory `brandmonkz_two_divergent_repos` (techcloudpro.com is domain-verified in Resend; May 26 Peter→Sara swap).

**Edit `backend/src/routes/apollo.ts` — ADD imports + module-load fail-fast + the new route handler.** The file already exists from Task 1. Add to it; do not create a second route file.

**Step 3a — Top of file (after existing imports from Task 1):**

```typescript
// Phase 4 USER-LOCKED 2026-05-30: Resend send path.
// We use Resend, NOT SES (campaigns.ts SES flow stays untouched for Rajesh's existing BrandMonkz campaigns).
import { Resend } from 'resend';

// Fail-fast at module load if RESEND_API_KEY missing. Pattern mirrors main_new.py JWT_SECRET guard
// (`RuntimeError` at startup). Backend MUST NOT boot without a working send path.
const RESEND_API_KEY = process.env.RESEND_API_KEY;
if (!RESEND_API_KEY) {
  console.error('[apollo.send-campaign] FATAL: RESEND_API_KEY env var is not set. Backend cannot start.');
  console.error('  → Add RESEND_API_KEY=re_... to backend/.env (live key in EC2 /var/www/crm-backend/.env or Resend dashboard).');
  process.exit(1);
}
const resend = new Resend(RESEND_API_KEY);

// Phase 4 from-address. Per-stream / configurable deferred to Phase 4.5.
// techcloudpro.com is domain-verified in Resend (May 26, 2026 Peter→Sara swap).
const APOLLO_FROM_EMAIL = 'Sara <sara@techcloudpro.com>';

// Canonical stream allowlist (mirrors STREAMS export from lib/streamClassifier.ts).
// Used to validate `suggestedStream` in the send-campaign request body.
const VALID_STREAMS = new Set([
  'NetSuite', 'AI/ML', 'Cloud/DevOps', 'Cybersecurity',
  'Data/Analytics', 'Mobile', 'Enterprise/ERP', 'Staffing/HR', 'Other',
]);
```

**Step 3b — Request/response interfaces (place near `ApolloImportRequestBody`):**

```typescript
interface ApolloSendCampaignRequestBody {
  contactIds: string[];
  templateId: string;
  suggestedStream: string;
}

interface ApolloSendCampaignFailure {
  contactId: string;
  email: string | null;
  error: string;
}

interface ApolloSendCampaignResponse {
  sent: number;
  failed: number;
  failureDetails: ApolloSendCampaignFailure[];
}
```

**Step 3c — Route handler (place AFTER the `/import` handler, BEFORE `export default router;`):**

```typescript
// POST /api/apollo/send-campaign
// Phase 4 USER-LOCKED send path. Dispatches campaign emails via Resend (NOT SES).
// Mirrors campaigns.ts:546-559 substitution logic but uses resend.emails.send instead of SESClient.
// Returns aggregated success/failure — never fail-fast on per-contact errors.
router.post('/send-campaign', async (req: Request, res: Response) => {
  const { contactIds, templateId, suggestedStream } = (req.body || {}) as ApolloSendCampaignRequestBody;

  // ===== Validation =====
  if (!Array.isArray(contactIds) || contactIds.length === 0) {
    return res.status(400).json({ error: 'contactIds must be a non-empty array' });
  }
  if (!templateId || typeof templateId !== 'string') {
    return res.status(400).json({ error: 'templateId required' });
  }
  if (!suggestedStream || !VALID_STREAMS.has(suggestedStream)) {
    return res.status(400).json({
      error: `suggestedStream must be one of: ${[...VALID_STREAMS].join(', ')}`,
    });
  }

  const userId = (req as any).user?.id || (req as any).user?.sub;
  if (!userId) return res.status(401).json({ error: 'unauthenticated' });

  try {
    // ===== Lookup template (must belong to caller) =====
    const template = await prisma.emailTemplate.findFirst({
      where: { id: templateId, userId },
    });
    if (!template) {
      return res.status(404).json({ error: 'Template not found or not owned by user' });
    }
    const tplSubject = template.subject || '';
    const tplBody = (template as any).htmlBody || (template as any).body || '';
    if (!tplSubject || !tplBody) {
      return res.status(400).json({ error: 'Template has no subject or body' });
    }

    // ===== Lookup contacts (must belong to caller; include company for {{companyName}}) =====
    const contacts = await prisma.contact.findMany({
      where: { id: { in: contactIds }, userId },
      include: { company: { select: { name: true } } },
    });

    if (contacts.length === 0) {
      return res.status(404).json({ error: 'No matching contacts found for caller' });
    }

    // ===== Send loop (sequential with 100ms gap — Resend free tier rate limit ~2/sec) =====
    const failureDetails: ApolloSendCampaignFailure[] = [];
    let sent = 0;

    for (const contact of contacts) {
      // Per-contact try/catch — collect failures, never fail-fast.
      try {
        if (!contact.email) {
          failureDetails.push({
            contactId: contact.id,
            email: null,
            error: 'contact has no email',
          });
          continue;
        }

        // Variable substitution — mirror campaigns.ts:546-559 pattern exactly.
        const vars: Record<string, string> = {
          firstName: contact.firstName || '',
          lastName: contact.lastName || '',
          email: contact.email,
          companyName: contact.company?.name || '',
        };
        let subject = tplSubject;
        let html = tplBody;
        for (const [key, val] of Object.entries(vars)) {
          const regex = new RegExp(`\\{\\{${key}\\}\\}`, 'g');
          subject = subject.replace(regex, val);
          html = html.replace(regex, val);
        }

        const result = await resend.emails.send({
          from: APOLLO_FROM_EMAIL,
          to: contact.email,
          subject,
          html,
        });

        // Resend SDK returns { data: { id }, error } — error is non-null on failure
        if ((result as any).error) {
          failureDetails.push({
            contactId: contact.id,
            email: contact.email,
            error: String((result as any).error?.message || (result as any).error),
          });
          continue;
        }

        sent++;

        // 100ms pacing — Resend free tier ~2/sec
        await new Promise<void>((r) => setTimeout(r, 100));
      } catch (err: any) {
        failureDetails.push({
          contactId: contact.id,
          email: contact.email ?? null,
          error: err?.message || 'unknown error',
        });
      }
    }

    const body: ApolloSendCampaignResponse = {
      sent,
      failed: failureDetails.length,
      failureDetails,
    };
    return res.status(200).json(body);
  } catch (err: any) {
    console.error('[apollo.send-campaign] unexpected error', err);
    return res.status(500).json({ error: 'send-campaign failed', detail: err?.message });
  }
});
```

**CRITICAL non-goals (do NOT do any of these):**
- DO NOT import from `@aws-sdk/client-ses` in `apollo.ts`. The verify block greps for this and fails if present.
- DO NOT modify `backend/src/routes/campaigns.ts` (Rajesh's existing SES path stays untouched).
- DO NOT modify `backend/src/services/awsSES.ts` if present.
- DO NOT make from-address configurable / per-stream — hardcoded `Sara <sara@techcloudpro.com>` is the locked Phase 4 decision.
- DO NOT add a tracking pixel — Resend has its own open/click tracking via dashboard; phase 4 does NOT wire CRM tracking_pixel into this path.
- DO NOT create a Campaign row in DB — wizard does not persist campaigns in Phase 4 (tracking happens via Contact.source='apollo'). Future phase may add an `email_logs` write if Rajesh asks.
- DO NOT do server-side `{{firstName}}` substitution on the WIZARD side. Substitution happens HERE, per-recipient.
  </action>
  <verify>
```bash
# 1. Resend SDK is imported (locked path)
grep -n "from 'resend'" /Users/jeet/production-crm/backend/src/routes/apollo.ts
# Expected: 1 match

# 2. The new route handler is registered
grep -n "router.post.*'/send-campaign'" /Users/jeet/production-crm/backend/src/routes/apollo.ts
# Expected: 1 match

# 3. From-address is the hardcoded Sara value
grep -n "sara@techcloudpro.com" /Users/jeet/production-crm/backend/src/routes/apollo.ts
# Expected: 1 match

# 4. Fail-fast guard for RESEND_API_KEY exists
grep -nE "process\.exit\(1\)|RESEND_API_KEY" /Users/jeet/production-crm/backend/src/routes/apollo.ts
# Expected: 2+ matches (the if-guard + the variable read)

# 5. NEGATIVE CHECK — SES MUST NOT be imported in apollo.ts (this is the locked-decision firewall)
grep -c "@aws-sdk/client-ses" /Users/jeet/production-crm/backend/src/routes/apollo.ts
# Expected: 0

# 6. NEGATIVE CHECK — campaigns.ts file must NOT have been modified by Task 3
git diff backend/src/routes/campaigns.ts | wc -l
# Expected: 0

# 7. TypeScript clean
cd /Users/jeet/production-crm/backend && npx tsc --noEmit 2>&1 | grep "apollo.ts" | head -10
# Expected: 0 errors related to apollo.ts

# 8. Boot-time fail-fast smoke (without RESEND_API_KEY)
cd /Users/jeet/production-crm/backend && unset RESEND_API_KEY && timeout 5 npm run dev 2>&1 | grep -E "FATAL.*RESEND_API_KEY|exit"
# Expected: at least 1 match — server should refuse to start
```
  </verify>
  <done>
apollo.ts has BOTH `/import` AND `/send-campaign` route handlers; Resend SDK imported; sara@techcloudpro.com hardcoded; RESEND_API_KEY missing crashes backend at boot; @aws-sdk/client-ses NOT imported in apollo.ts; campaigns.ts unchanged; tsc clean.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "router.post.*'/import'" backend/src/routes/apollo.ts` returns 1 match
2. `grep -n "router.post.*'/send-campaign'" backend/src/routes/apollo.ts` returns 1 match
3. `grep -n "/api/apollo" backend/src/app.ts` returns 1 match
4. `grep -n "seed-streams" backend/src/routes/email-templates.ts` returns 1 match
5. `grep -n "from 'resend'" backend/src/routes/apollo.ts` returns 1 match
6. `grep -c "@aws-sdk/client-ses" backend/src/routes/apollo.ts` returns 0 (firewall: SES MUST NOT bleed into Apollo path)
7. `git diff backend/src/routes/campaigns.ts` returns no output (existing SES path untouched)
8. `cd backend && npx tsc --noEmit` exits 0
9. Local POST /api/apollo/import without APOLLO_API_KEY returns 503 with "Apollo not configured"
10. Local POST /api/email-templates/seed-streams creates 9 rows on first call, 0 on repeat call (idempotent)
11. Local GET /api/email-templates?category=Stream:NetSuite returns 1 row
12. Backend refuses to start without RESEND_API_KEY (fail-fast at boot)
</verification>

<success_criteria>
- /api/apollo/import is wired and handles missing-key, invalid-key, dedup, classify, upsert correctly
- /api/apollo/send-campaign is wired and dispatches via Resend with Sara hardcoded from-address
- Per-contact substitution mirrors campaigns.ts:546-559 logic
- 9 stream-specific EmailTemplate rows seedable per user, idempotently
- GET /api/email-templates?category= filter works so the wizard in plan 04-05 can find templates
- All routes pass authentication
- @aws-sdk/client-ses is NOT imported in apollo.ts (firewall maintained)
- campaigns.ts SES path is byte-for-byte unchanged (Rajesh's BrandMonkz flow untouched)
- Backend boot fails loudly if RESEND_API_KEY missing (no silent send failures in production)
</success_criteria>

<output>
After completion, create `.planning/phases/04-apollo-import-and-auto-campaign/04-03-SUMMARY.md` with:
- HTTP request/response examples for /api/apollo/import AND /api/apollo/send-campaign
- Confirmation seed-streams is idempotent
- Field-name mapping diffs (what was in the skeleton vs what actually matched the schema)
- Confirmation that campaigns.ts was NOT touched (git diff empty)
- Confirmation that @aws-sdk/client-ses is NOT in apollo.ts (grep -c = 0)
- Pre-condition reminder: APOLLO_API_KEY + RESEND_API_KEY must BOTH be in EC2 .env before plan 04-06 smoke runs
</output>
</output>
