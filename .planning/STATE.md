# Project State

Last activity: 2026-06-01 - Phase 6 Plan 05 (Apollo Campaign button + Pending Review tab + apolloApi client bindings) COMPLETE. Single atomic commit `9bb3311` on `production` by jeet-avatar <jm@techcloudpro.com> — 3 frontend files changed, 678 insertions, 18 deletions: (1) frontend/src/services/api.ts — 5 new typed apolloApi methods (`unsentContacts({stream?, limit?})` + `pendingReview.list/approve/reject/edit`) + 7 new interfaces (`ApolloUnsentContact`, `ApolloUnsentContactsResponse`, `ApolloPendingReviewItem`, `ApolloPendingReviewListResponse`, `ApolloPendingReviewApproveResponse`, `ApolloPendingReviewRejectResponse`, `ApolloPendingReviewEditResponse`) + 2 new optional fields on existing Phase 5 types (`ApolloSendPersonalizedRequest.requireReview?: boolean`, `ApolloPersonalizedCampaignResponse.queuedForReview?: number` + `queueIds?: string[]`); (2) frontend/src/components/PendingReviewQueue.tsx (NEW, 411 LOC) — named export, owns own data fetching + pagination (pageSize=20) + per-row mutation state + bulk-approve progress + inline Reject confirmation (maxLength=500) + Edit token modal; inbox-card preview uses `dangerouslySetInnerHTML={{__html: DOMPurify.sanitize(item.renderedBody)}}` inside `max-h-[400px] overflow-y-auto` scroll container; empty-state copy verbatim per CONTEXT.md line 111 ("No pending emails." + "Click \"Apollo Campaign\" above to generate the next batch."); Edit modal calls `apolloApi.pendingReview.edit({aiTokens})` only and patches local item with response.renderedBody (server-re-rendered per Plan 06-04 MEDIUM 1 fix); zero direct fetch/axios/apiClient imports; zero NetSuiteCampaignWizard import; (3) frontend/src/pages/Campaigns/CampaignsPage.tsx — added `RocketLaunchIcon` to existing heroicons import, added `apolloApi + emailTemplatesApi` static import (HIGH 3 fix: NO `_notRealMethod_` placeholder, NO `await import()` inside click handler — both grep -c = 0), added `PendingReviewQueue` import, added module-scope `VALID_STREAMS_FRONTEND` const mirroring backend apollo.ts:73-76 VALID_STREAMS exactly (NetSuite, AI/ML, Cloud/DevOps, Cybersecurity, Data/Analytics, Mobile, Enterprise/ERP, Staffing/HR, Other — all 9 grep -c ≥ 1, all invented streams RPA/Web3/AI-ML grep -c = 0), added 4 new state vars (`view`, `stagingQueue`, `stagingStreams`, `pendingRefreshKey`), added `handleApolloCampaignClick` async handler implementing: (a) `apolloApi.unsentContacts()` fetch, (b) HARD_CAP=200 + LARGE_CONFIRM=50 gate with window.confirm, (c) group-by-stream with VALID_STREAMS_FRONTEND guard routing rogue streams to 'Other' via console.warn (BLOCKER 3 fix), (d) defensive post-grouping assertion throws if any byStream key escapes VALID_STREAMS_FRONTEND, (e) per-stream `emailTemplatesApi.findByCategory('Stream:'+streamName)` lookup (NO-space format matches stream-templates.ts:102 seed.category writes — grep -c "'Stream: " with space = 0), (f) fallback to `'Stream:Other'` when exact match missing, (g) per-group `apolloApi.sendPersonalizedCampaign({contactIds, templateId, suggestedStream, requireReview:true, confirmedLargeBatch: group.length > 50})` dispatch, (h) `setView('pendingReview')` + `setPendingRefreshKey(k=>k+1)` auto-switch on success; added Apollo Campaign button INSIDE existing `<div className="flex items-center gap-3">` header gradient block BEFORE Create Campaign button (line-number proof: Apollo grep first hit = line 28, Create grep first hit = line 395, $APOLLO_LINE < $CREATE_LINE confirmed) — orange-to-amber gradient + RocketLaunchIcon distinguishes it from indigo Create Campaign, label switches to "Staging N/M streams…" during dispatch; added view toggle row (Campaigns | Pending Review) ABOVE existing status filter chips, status filter row + body now gated on `view === 'campaigns'` (default tab unchanged); PendingReviewQueue mounted when `view === 'pendingReview'` with `refreshKey={pendingRefreshKey}` wire. All 3 files in single atomic commit (Task 1 + Task 2 + Task 3 squashed per plan STEP G — frontend UI surface is one logical change). Verification GREEN: 3/3 files in commit (`git diff HEAD~1..HEAD --stat | grep -cE "(api\.ts|CampaignsPage\.tsx|PendingReviewQueue\.tsx)" = 3`); 0 backend changes in commit; tsc --noEmit clean on all 3 modified files (`cd frontend && npx tsc --noEmit -p . 2>&1 | grep -E "(CampaignsPage|PendingReviewQueue|services/api)" returns 0 lines`); 7 occurrences of "Apollo Campaign" string; 2 occurrences of "RocketLaunchIcon" (import + button); 6 occurrences of `'pendingReview'`; 2 occurrences of `handleApolloCampaignClick` (def + onClick wiring); 1 `apolloApi.unsentContacts`; 2 `apolloApi.sendPersonalizedCampaign` (existing wizard reference + new handler); 2 `requireReview: true`; 3 `emailTemplatesApi.findByCategory`; 5 `VALID_STREAMS_FRONTEND`; 1 `<PendingReviewQueue`; 1 import path; all 9 real prod streams grep -c = 1 each (Other = 7 due to fallback usage); all 3 invented streams grep -c = 0; `'Stream:'+streamName` grep -c = 1; `'Stream:Other'` grep -c = 1; PendingReviewQueue: 1 named export, 1 list call, 2 approve calls (single + bulk), 1 reject call, 1 edit call, 1 DOMPurify.sanitize, 1 'No pending emails', 1 'Click "Apollo Campaign"', 1 `updated.renderedBody`, 2 're-rendered server-side', 0 NetSuiteCampaignWizard, 0 axios/apiClient. Phase 4 SES firewall HELD byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`. Phase 5 send-path firewall HELD: `git diff HEAD~1..HEAD -- backend/src/routes/apollo.ts | wc -l = 0`. NetSuiteCampaignWizard.tsx 0-touch verified: `git diff HEAD~1..HEAD -- frontend/src/components/NetSuiteCampaignWizard.tsx | wc -l = 0`. Backend untouched in commit: `git diff HEAD~1..HEAD --stat | grep -cE "^\s*backend/" = 0`. Schema untouched: `git diff HEAD~1..HEAD --stat | grep -cE "schema\.prisma|migrations/" = 0`. Push fast-forward `d60d845..9bb3311` to origin/production. Single commit, 3 files, NO deviations from plan (executed STEP 0 pre-flight + Task 1 STEP A-C + Task 2 verbatim component + Task 3 STEP A-G verbatim — no Rule-1/2/3/4 triggered, no auth gate, no architectural decision). Closes REQ-064 + REQ-065. Approval-citation note (per CONTEXT.md rule): plan frontmatter `autonomous: true` — no approval required. Plan 06-05 SUMMARY at `.planning/phases/06-stream-coherent-templates-and-review-queue/06-05-SUMMARY.md`. Wave 4 of Phase 6 NOW COMPLETE (6/7 plans done; 06-01a + 06-01b + 06-02 + 06-03 + 06-04 + 06-05). Wave 5 = Plan 06-06 (deploy gate with `autonomous: false` + 2 checkpoint:human-action approvals (deploy approval + live-verify approval) + 9-stream live verification with Rajesh approval at least 1 row) — BLOCKED until user explicitly types `deploy` per CONTEXT.md "🛑 DEPLOY-TO-LIVE REQUIRES EXPLICIT USER APPROVAL" hard rule.

Prior activity: 2026-06-02 - Phase 6 Plan 04 (5 new backend pending-review CRUD routes on /api/apollo) COMPLETE. Single atomic commit `d8b3350` on `production` by jeet-avatar <jm@techcloudpro.com> — 1 file changed (backend/src/routes/apollo.ts), 313 insertions, 0 deletions. Pre-flight schema check PASSED: `grep -c "renderedBody\s*String" backend/prisma/schema.prisma = 1` (PersonalizedEmailSend.renderedBody at line 2357), `grep -c "fromEmail\s*String" backend/prisma/schema.prisma = 5` (PersonalizedEmailSend.fromEmail at line 2353 among others) — Phase 5 schema dependency confirmed before any route code written. Task 1 + Task 2 squashed into ONE atomic commit per plan STEP D-E instruction. All 5 new route handlers added between existing /send-personalized-campaign route (ends line 1154) and `export default router;`: (1) `GET /api/apollo/unsent-contacts` — two-step query (collect sent contactIds for userId+status='sent', then findMany contacts where source='apollo' AND userId AND id NOT IN sentContactIds with optional ?stream filter + ?limit clamped 1..1000 default 500), maps to `{ contacts: [{id, email, fullName, companyName, stream, suggestedStream}], total }` — used by Plan 06-05's Apollo Campaign button to batch-fetch unsent prospects; (2) `GET /api/apollo/pending-review` — paginated `findMany` + `count` via `prisma.$transaction([])` where userId AND status='pending_review', optional ?stream filter, ?page default 1, ?pageSize default 20 clamped 1..100, includes contact.company relation for contactName/companyName derivation, response shape `{ items: [{id, contactId, contactEmail, contactName, companyName, stream, subject, renderedBody, aiTokens, aiWarning, claudeCostUSD, createdAt}], total, page, pageSize }` (resendError deliberately excluded from response to avoid leaking server-only field); (3) `POST /api/apollo/pending-review/:id/approve` — single findFirst({id, userId, status:'pending_review'}) gate that atomically catches missing-row AND wrong-status (locks out double-approval), `resend.emails.send({ from: row.fromEmail || APOLLO_FROM_EMAIL, to: row.toEmail, subject: row.subject, html: row.renderedBody })` re-uses persisted body (NO fresh Claude call — grep verified 0 occurrences of personalizeContactWithClaude inside handler block), on success updates to status='sent' with resendMessageId + sentAt=NOW(), on Resend error returns 502 LEAVING row in status='pending_review' so user can retry (matches must_have truth), 404 returns 'pending_review_row_not_found'; (4) `POST /api/apollo/pending-review/:id/reject` — accepts `{ reason?: string }`, same findFirst gate, updates status='rejected' AND writes reason into EXISTING aiWarning column (re-use avoids schema change — CONTEXT.md route table treats reason as optional metadata), when reason not provided preserves existing aiWarning value, no Resend dispatch; (5) `POST /api/apollo/pending-review/:id/edit` — accepts partial `{ aiTokens?, renderedBody?, subject? }` with 400 'no_fields_to_edit' if all undefined, same findFirst gate including contact+company relation (re-rendering needs firstName/title/company.name), **MEDIUM 1 FIX server-side re-render path**: when `aiTokens !== undefined && renderedBody === undefined`, re-loads `prisma.emailTemplate.findFirst({id: row.templateId, userId, select: {htmlContent: true}})`, returns 409 `template_missing_for_rerender` if template deleted (workaround hint: edit renderedBody directly), builds 7-placeholder map `{firstName, companyName, title, intentHook, companyContext, painPoint, cta}` from row.contact + aiTokens, runs `body.replace(new RegExp(\`\\{\\{${key}\\}\\}\`, 'g'), safeValue)` for each key — IDENTICAL substitution semantics to /send-personalized-campaign loop at apollo.ts:1002-1006 (regex global flag, double-brace match); when renderedBody explicitly provided that value wins (manual edit preserved, no re-render), persists ONLY provided fields via Prisma partial update with `Prisma.PersonalizedEmailSendUpdateInput` + `Prisma.InputJsonValue` cast on aiTokens matching Phase 5 pattern at apollo.ts:1023-1025, status stays 'pending_review'. ALL 5 ROUTES enforce userId tenant isolation via Prisma WHERE clause (defense in depth — `router.use(authenticate)` at line 82 already gates the prefix, but every Prisma op re-asserts userId so IDOR is impossible even if middleware regresses). All 3 mutation routes use SINGLE `findFirst({where: {id, userId, status: 'pending_review'}})` instead of separate row-fetch + post-fetch status check — atomic intent prevents racing double-approval. Verification GREEN: 5/5 router declarations added (`git diff HEAD~1..HEAD -- apollo.ts | grep -c "^+router\." = 5`), 0/0 new personalizeContactWithClaude calls added in approve handler block, /approve grep -A 50 contains row.renderedBody, /edit grep -A 60 contains template_missing_for_rerender + prisma.emailTemplate.findFirst + newRenderedBody, /edit 7-placeholder set ≥ 7 occurrences, all 3 mutation routes contain `status: 'pending_review'` in findFirst window, all 5 routes contain userId reference within first 12 lines (tenant isolation pattern). 5-fallback Claude contract PRESERVED byte-for-byte: personalizeContactWithClaude helper at lines 222-322 untouched (0 lines added inside the body); 5/5 distinct warning strings still emit exactly once each from helper body — 'Claude not configured' (line 228), 'Claude timed out — used per-stream fallback' (263), 'Claude returned empty response' (279), 'Claude returned malformed JSON' (298), 'Claude unavailable' (313); whole-file grep counts are higher because new comment blocks reference some warning strings but helper-body-scoped grep is the authoritative measure. Backend `tsc --noEmit` reports ZERO new errors in apollo.ts (8 pre-existing baseline errors in apiSubscriptions.controller.ts + analytics.routes.ts + subscriptions.ts + cronScheduler.ts + export.service.ts + publicCheckout.routes.ts tolerated per Phase 5 SUMMARY baseline). Phase 4 SES firewall HELD byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`. NetSuiteCampaignWizard.tsx 0-touch verified: `git diff HEAD~1..HEAD -- frontend/src/components/NetSuiteCampaignWizard.tsx | wc -l = 0`. Frontend untouched in commit: `git diff HEAD~1..HEAD --stat | grep -cE "frontend/" = 0`. Schema untouched in commit: `git diff HEAD~1..HEAD --stat | grep -cE "schema\.prisma|migrations/" = 0`. APOLLO_FROM_EMAIL + RESEND_COST_PER_SEND constants RE-USED (no new top-level constants). Push fast-forward `44b4eac..d8b3350` to origin/production. Single commit, single file, no deviations from plan (executed STEP 0 pre-flight + Task 1 STEP A-B + Task 2 STEP A-D verbatim — no Rule-1/2/3/4 triggered, no auth gate, no architectural decision). Closes REQ-063. Approval-citation note (per CONTEXT.md rule): user's verbatim "lets call it approved - and send email to jm@techcloudpro.com" (logged at Plan 06-01b) approved 9-stream copy drafts in 06-01a's 06-STREAM-COPY.md — NOT Plan 06-04's routes. Plan 06-04 frontmatter declares `autonomous: true`; no approval required. Plan 06-04 SUMMARY at `.planning/phases/06-stream-coherent-templates-and-review-queue/06-04-SUMMARY.md`. Wave 3 of Phase 6 NOW COMPLETE (5/7 plans done; 06-01a + 06-01b + 06-02 + 06-03 + 06-04). Wave 4 = Plan 06-05 (PendingReviewQueue.tsx component on CampaignsPage + Apollo Campaign button + apolloApi.unsentContacts + apolloApi.pendingReview.{list,approve,reject,edit} frontend bindings) UNBLOCKED — consumes all 5 routes added by 06-04.

Prior activity: 2026-06-01 - Phase 6 Plan 01b (per-stream STREAM_TEMPLATE_V3_BODIES dict synthesis post-user-approval checkpoint) COMPLETE. Single atomic commit `01fe98a` on `production` by jeet-avatar <jm@techcloudpro.com> — 2 files changed (backend/src/seeds/stream-templates.ts + backend/src/routes/emailTemplates.ts), 254 insertions, 70 deletions. **User approval (verbatim, captured per CONTEXT.md citation rule):** "lets call it approved - and send email to jm@techcloudpro.com" — first clause interpreted by orchestrator as the literal `copy-approved` token required by Task 1's checkpoint:human-action gate; second clause handled separately by orchestrator after this plan's commit. Task 2 executed verbatim through STEP A→I: (A-B) parsed 9 sections of 06-STREAM-COPY.md + studied existing single-body STREAM_TEMPLATE_V3_BODY copy spans; (C) wrote internal `buildStreamV3Body(opts: StreamV3CopyOpts)` helper with 13-field opts (sentinel, preheaderTail, tagPillText, metricLabel, introSentence, noteCalloutTitle (null|OMIT skips block), noteCalloutBody, servicePropTitle1+Body1, servicePropTitle2+Body2, footerReprise, arthaBannerSubtitle, ariaBlurb) — preserves byte-for-byte ALL shared chrome (<head> mso fallbacks, top accent bar gradient, navy `#0F172A` header, 4-cell metrics row keeping `1000+` + `Implementations` + `Since 2015` + `94%` + `Faster Close` + `$1/contract` + `Staffing Fee`, orange `#FFF7ED` primary callout containing 4 AI <p> tags, service props 03 `Dedicated AI Consulting` + 04 `Transparent staffing $1 per contract`, 30-day guarantee, 4 CTA buttons Call ARIA + Call a Human + Visit TechCloudPro + Visit ArthaBuild AI, Sara signature, mailto:sara@techcloudpro.com unsubscribe footer); sentinel interpolated as FIRST line immediately after `<body>`; tagPillText/metricLabel/introSentence/footerReprise/arthaBannerSubtitle/ariaBlurb interpolated at exact swap-points; servicePropTitle1+Body1 + servicePropTitle2+Body2 interpolated into 01 + 02 slots; secondary "AI banner" callout block (gradient blue/indigo `#EFF6FF` "A note on NetSuite Next 2026") dropped entirely when noteCalloutTitle is null or 'OMIT'. (D) Defined `STREAM_TEMPLATE_V3_BODIES: Record<string, string>` (EXPORTED) as 9-entry dict with NO-SPACE keys matching prod DB writes from seed.category template at stream-templates.ts:102: 'Stream:NetSuite' (preserves quick-8 v3 copy verbatim + keeps 'A note on NetSuite Next 2026' secondary callout), 'Stream:AI/ML', 'Stream:Cloud/DevOps', 'Stream:Cybersecurity', 'Stream:Data/Analytics', 'Stream:Mobile', 'Stream:Enterprise/ERP', 'Stream:Staffing/HR', 'Stream:Other'. (E) DELETED single-body `export const STREAM_TEMPLATE_V3_BODY = ...` const entirely. (F) Rewrote `upgradeStreamTemplatesToV3` to per-row stream-aware dict lookup: derives bareStream via `row.category.slice('Stream:'.length)`, builds sentinel `<!-- STREAM_V3:<bareStream> -->`, checks `htmlContent.includes(sentinel)` for idempotency, falls back to `STREAM_TEMPLATE_V3_BODIES['Stream:Other']` for unknown categories. (G) Updated `/upgrade-streams-v3` endpoint doc comment in emailTemplates.ts (URL unchanged) to reference Phase 06 plan 06-01b + per-stream dict lookup + HTML-comment sentinel. (H) Build + runtime guard: `npm run build` succeeds with ZERO new errors in stream-templates.ts / emailTemplates.ts (8 pre-existing TS errors in apiSubscriptions.controller.ts + analytics.routes.ts + subscriptions.ts + cronScheduler.ts + export.service.ts tolerated per Phase 5 SUMMARY baseline); runtime guard `node -e "..."` prints `OK 9/9` + `CROSS-CONTAM CHECK: PASS` from dist/seeds/stream-templates.js — confirms every prod Stream:* seed has matching dict entry AND each entry contains its OWN sentinel AND NO other stream's sentinel. (I) Single atomic commit + push fast-forward `3f8ce4c..01fe98a` to origin/production. Per-stream body sizes (rendered HTML lengths): NetSuite 21,283 chars (only stream with secondary callout), AI/ML 19,567, Cloud/DevOps 19,580, Cybersecurity 19,584, Data/Analytics 19,623, Mobile 19,589, Enterprise/ERP 19,675, Staffing/HR 19,635, Other 19,531 — all ≥15,000-char threshold required by plan. All 31 verification greps pass: 1/1 STREAM_TEMPLATE_V3_BODIES export, 1/1 upgradeStreamTemplatesToV3 export, 1/1 buildStreamV3Body helper, 0/0/0 STREAM_TEMPLATE_V3_BODY single-body removal greps, 1/1 each for v2+seeds preservation (STREAM_TEMPLATE_V2_BODY + upgradeStreamTemplatesToV2 + getStreamFallbacks + STREAM_TEMPLATE_SEEDS + seedStreamTemplates), 1/1 each for 9 NO-SPACE dict keys, 0/0/0 invented streams (Stream:RPA + Stream:Web3 + Stream:AI-ML all 0), 0 'Stream: ' (with space), ≥1 each for 9 HTML-comment sentinels and 9 "X Practice" metricLabel literals, ≥1 each for 6 placeholders, 1 mailto:sara@techcloudpro.com, 1 router.post('/upgrade-streams-v3'), 1 Phase 06 plan 06-01b doc comment. Phase 4 SES firewall HELD byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`. Phase 5 send-path firewall HELD: `git diff HEAD~1..HEAD -- backend/src/routes/apollo.ts | wc -l = 0`. NetSuiteCampaignWizard.tsx 0-touch verified: `git diff HEAD~1..HEAD -- frontend/src/components/NetSuiteCampaignWizard.tsx | wc -l = 0`. Files NOT in commit (grep count must be 0): campaigns.ts + awsSES.ts + NetSuiteCampaignWizard + CampaignsPage + api.ts + schema.prisma + apollo.ts — all 0 confirmed. Single commit, 2 files, no deviations from plan (executed STEP A-I verbatim — no Rule-1/2/3/4 triggered). Closes REQ-060 (step 2/2 — step 1/2 was Plan 06-01a's 06-STREAM-COPY.md draft). Plan 06-01b SUMMARY at `.planning/phases/06-stream-coherent-templates-and-review-queue/06-01b-SUMMARY.md`. Wave 2 of Phase 6 NOW COMPLETE (06-01b + 06-03 both done; 06-01a + 06-02 from Wave 1 also done). Wave 3 (Plan 06-04 — 4 new pending-review routes: GET /pending-review + POST /:id/approve + POST /:id/reject + POST /:id/edit with server-side renderedBody re-render after aiTokens override) UNBLOCKED — can consume queueIds from 06-03's response envelope OR list-by-status='pending_review' (plan 06-04 likely uses list-by-status).

Prior activity: 2026-06-02 - Phase 6 Plan 03 (requireReview additive body field on POST /api/apollo/send-personalized-campaign) COMPLETE. Single atomic commit `3e38d37` on `production` by jeet-avatar <jm@techcloudpro.com> — 1 file changed (backend/src/routes/apollo.ts), 43 insertions, 1 deletion. Six surgical edits applied per plan STEP A-F: (A) destructured new optional body field `requireReview?: boolean` next to existing `previewOnly` + `confirmedLargeBatch`; (B) added in-loop accumulators `const queueIds: string[] = []` + `let queuedForReview = 0` next to the existing 10 counters; (C) status field on `prisma.personalizedEmailSend.create({...})` rewritten as 3-way ternary `requireReview ? 'pending_review' : (previewOnly ? 'preview' : 'pending')` — requireReview WINS over previewOnly when both true (mutual exclusion semantic locked); (D) NEW skip-Resend branch 4b inserted between previewOnly skip (4) and Resend dispatch (5): pushes auditRow.id to queueIds, increments queuedForReview, pushes audit envelope with status='pending_review', sleeps PERSONALIZE_PACING_MS=250ms inline before `continue` to preserve 4 req/sec ceiling on Claude API for 50-contact requireReview batches; (E) response envelope extended additively with two new top-level keys (`queuedForReview: number`, `queueIds: string[]`) placed BEFORE `cost: {}` — old callers iterate keys without breaking; (F) route header comment block extended with Phase 06 additive-extension paragraph documenting field semantics + mutual-exclusion rule. Verification GREEN: requireReview occurrences = 10 (plan expected ≥5); 'pending_review' occurrences = 6 (plan expected ≥2); queuedForReview = 4 (plan expected ≥3); queueIds = 4 (plan expected ≥3); `if (requireReview)` count = 1 (plan expected ≥1); `requireReview ? 'pending_review'` count = 1 (plan expected =1); zero `^-` lines removed from existing route declarations (/send-campaign, /import, /normalize-filters). 5-fallback Claude contract PRESERVED byte-for-byte — `personalizeContactWithClaude` helper (lines 222-322) untouched, 5/5 distinct warning strings still emit: 'Claude not configured' (line 228), 'Claude timed out — used per-stream fallback' (263), 'Claude returned empty response' (279), 'Claude returned malformed JSON' (298), 'Claude unavailable' (313). Backend tsc --noEmit reports ZERO new errors in apollo.ts (8 pre-existing unrelated errors in apiSubscriptions.controller.ts + analytics.routes.ts + subscriptions.ts + cronScheduler.ts + export.service.ts tolerated per Phase 5 SUMMARY baseline). Phase 4 SES firewall HELD: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`. NetSuiteCampaignWizard.tsx 0-touch verified: `git diff frontend/src/components/NetSuiteCampaignWizard.tsx | wc -l = 0` AND `git diff HEAD~1 frontend/src/components/NetSuiteCampaignWizard.tsx | wc -l = 0` — the wizard's existing call `apolloApi.sendPersonalizedCampaign({contactIds, templateId, suggestedStream, confirmedLargeBatch})` never passes requireReview → undefined → falsy → existing Resend dispatch path runs unchanged byte-for-byte. Other firewalled surfaces also 0-touch in HEAD~1..HEAD: campaigns.ts, awsSES.ts, CampaignsPage.tsx, api.ts, schema.prisma, stream-templates.ts, emailTemplates.ts. Single commit, single file, no deviations from plan (executed STEP A-F verbatim — no Rule-1/2/3/4 triggered). Push fast-forward `7306b95..3e38d37` to origin/production. Closes REQ-062. Plan 06-03 SUMMARY at `.planning/phases/06-stream-coherent-templates-and-review-queue/06-03-SUMMARY.md`. Wave 2 of Phase 6 partially complete (Plan 06-03 done in parallel with Plan 06-01b which still awaits user `copy-approved` checkpoint on 06-STREAM-COPY.md before dict synthesis can run). Wave 3 (Plan 06-04 — 4 new pending-review routes: GET /pending-review + POST /:id/approve + POST /:id/reject + POST /:id/edit) UNBLOCKED upon Plan 06-01b completion; 06-04 can consume queueIds from 06-03's response envelope OR list-by-status='pending_review' (both work, plan 06-04 likely uses list-by-status).

Prior activity: 2026-06-02 - Phase 6 Plan 01a (write 06-STREAM-COPY.md — 9-stream human-readable copy drafts) COMPLETE. Single atomic commit `f9c909f` on `production` by jeet-avatar <jm@techcloudpro.com> — 1 file changed, 268 insertions, 0 source code touched (backend/frontend/prisma all clean). Created `.planning/phases/06-stream-coherent-templates-and-review-queue/06-STREAM-COPY.md` (~18.4 KB) with: file-level preamble (what / why / how-user-approves), naming-conventions block (NO-SPACE dict-key format `'Stream:NetSuite'` matching seed.category writes at stream-templates.ts:102, HTML-comment sentinel format `<!-- STREAM_V3:<bareName> -->`, OMIT-marker semantics for non-NetSuite callout), shared-visual-chrome block (navy `#0F172A` header, orange `#F97316` accent, 4-cell metrics row, 4 CTA buttons, Sara signature), 12-per-stream-fields enumeration, shared-service-props-03+04 block (Dedicated AI Consulting + Transparent staffing $1/contract), 9 stream sections in VALID_STREAMS order (NetSuite preserved verbatim from quick-8 v3 with `A note on NetSuite Next 2026` inline callout intact; AI/ML, Cloud/DevOps, Cybersecurity, Data/Analytics, Mobile, Enterprise/ERP, Staffing/HR, Other all newly drafted with `noteCalloutTitle: OMIT` dropping the callout block entirely), trailing block (how-to-approve, how-to-edit, verification-before-06-01b-unblocked). Each section carries: dict-key header verbatim (`'Stream:NetSuite'`, `'Stream:AI/ML'`, etc., NO space), HTML-comment sentinel header verbatim (`<!-- STREAM_V3:NetSuite -->`, `<!-- STREAM_V3:AI/ML -->`, etc., bare-stream suffix), 12-row field table (preheaderTail, tagPillText, metricLabel, introSentence, noteCalloutTitle, noteCalloutBody, servicePropTitle1+Body1, servicePropTitle2+Body2, footerReprise, arthaBannerSubtitle, ariaBlurb). Per-stream metricLabel literal is each stream's name + ` Practice` (NetSuite Practice / AI/ML Practice / Cloud/DevOps Practice / Cybersecurity Practice / Data/Analytics Practice / Mobile Practice / Enterprise/ERP Practice / Staffing/HR Practice / TCP Practice for Stream:Other) — unique-by-construction, will serve as Plan 06-06's psql per-stream sentinel grep anchor. All verification gates pass: 9/9 section headers `^## Stream:<name>$`, 9/9 HTML-comment sentinels present, 9/9 unique metricLabel literals, `A note on NetSuite Next 2026` present in Stream:NetSuite only, OMIT count >=8 (actually 12 — 8 noteCalloutTitle OMITs + 4 ignored-callout-body references), NO invented streams (zero hits for Stream:RPA, Stream:Web3, Stream:AI-ML), correct NO-SPACE dict-key format throughout (zero `'Stream: ` with space). Push fast-forward `eb6dd24..f9c909f` to origin/production. REQ-060 step 1/2 closed; step 2/2 (mechanical dict-literal synthesis + stream-aware /upgrade-streams-v3 rewire) belongs to Plan 06-01b after `copy-approved` human-action checkpoint. Wave 1 of Phase 6 complete (both 06-01a + 06-02 done in parallel); Wave 2 (06-01b copy-approved checkpoint + dict synthesis + 06-03 requireReview additive field) UNBLOCKED. Note on verify-spec edge case: plan's verify-block `grep -cE "Stream:Cloud\b"` and `Stream:Data\b"` both returned 3 instead of expected 0 because `\b` matches between word/non-word and `/` is a word boundary — hits are content-correct (preamble historical-name backticks + legitimate `Stream:Cloud/DevOps` and `Stream:Data/Analytics` section headers); no actual invented bare-Cloud or bare-Data streams present. SUMMARY at `.planning/phases/06-stream-coherent-templates-and-review-queue/06-01a-SUMMARY.md`.

Prior activity: 2026-06-01 - Phase 6 Plan 02 (PersonalizedEmailSend.status pending_review + rejected migration) COMPLETE. Single atomic commit `eb6dd24` on `production` (jeet-avatar <jm@techcloudpro.com>) — 2 files changed, 17 insertions(+), 1 deletion(-). Task 1 updated the inline comment on `PersonalizedEmailSend.status` at backend/prisma/schema.prisma:2366 from `'pending' | 'sent' | 'failed' | 'preview'` → `'pending' | 'preview' | 'sent' | 'failed' | 'pending_review' | 'rejected'  (Phase 06: pending_review + rejected added)` — surgical 1-line diff, field declaration / default / attributes byte-identical, 0 model-count delta, Prisma 5.4.2 validate clean. Task 2 created `backend/prisma/migrations/20260601120000_phase06_pending_review_status/migration.sql` (357 bytes) — no-op SELECT marker (`SELECT 'Phase 06 plan 02: status column now admits pending_review + rejected (additive, no DDL)' AS phase06_marker;`) wrapped in 14 lines of comments documenting why no DDL is needed (status column is TEXT not Postgres ENUM, so adding string values is pure documentation at DB layer). Force-added past `backend/.gitignore:49` via `git add -f` per established Phase 04-01 + 03.1-02 + 03.1-03 + 05-01 precedent. Both tasks combined into ONE atomic commit per Task 2 STEP D-E instruction (single logical schema-status change). Verified: 0 ALTER TABLE, 0 CREATE TABLE, 0 DROP statements in migration.sql; `git ls-files` confirms migration.sql tracked; older `20260531120000_phase05_personalized_email_send/migration.sql` untouched. Phase 4 firewall PRESERVED byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`. apollo.ts NOT touched in HEAD~1..HEAD (route changes land in Plans 06-03 + 06-04). UI surfaces (NetSuiteCampaignWizard / CampaignsPage / api.ts) NOT touched (changes land in Plan 06-05). Migration .sql committed but NOT applied to prod DB — `prisma migrate deploy` deferred to Plan 06-06 deploy gate per locked DEPLOY-TO-LIVE protocol. Push fast-forward `c13cd41..eb6dd24` to origin/production. Closes REQ-061. Plan 06-02 SUMMARY at `.planning/phases/06-stream-coherent-templates-and-review-queue/06-02-SUMMARY.md`. Wave 1 of Phase 6 now has Plan 06-02 done; Plan 06-01a (write 06-STREAM-COPY.md draft) runs in parallel. Wave 2 (06-01b copy-approved checkpoint + dict synthesis, 06-03 requireReview additive field) UNBLOCKED upon 06-01a completion.

Prior activity: 2026-06-01 - Phase 6 PLANNED & VERIFIED. /gsd:plan-phase 6 ran end-to-end through the orchestrator (research skipped — CONTEXT.md authoritative, planner spawned with full files_to_read + REQ list + recommended structure, checker run iteration 0 found 4 BLOCKERs + 3 HIGH + 2 MEDIUM + 1 LOW, orchestrator corrected the design-doc-level BLOCKERs in-place (wrong 9-stream taxonomy fixed in 06-CONTEXT.md + ROADMAP.md + memory file at commit 73660e0 — replaced invented RPA/Web3/Data/Cloud/AI-ML with the 9 REAL prod streams NetSuite/AI/ML/Cloud/DevOps/Cybersecurity/Data/Analytics/Mobile/Enterprise/ERP/Staffing/HR/Other; replaced 'Stream: <name>' space format with NO-SPACE 'Stream:<name>' matching seed-code category writes at stream-templates.ts:102; switched sentinel pattern from visible-copy uniqueness to <!-- STREAM_V3:<suffix> --> HTML-comment for deterministic per-stream idempotency; added runtime guard requirement), planner revision iteration 1 made targeted plan-level fixes (Plan 06-01 split into 06-01a writing 06-STREAM-COPY.md draft + 06-01b autonomous:false with copy-approved checkpoint then dict synthesis per HIGH 2; 06-04 /edit re-renders renderedBody server-side after aiTokens override per MEDIUM 1; 06-04 STEP 0 pre-flight greps schema columns per HIGH 1; 06-05 STEP 0 pre-flight confirms emailTemplatesApi.findByCategory shape + static import replaces _notRealMethod_ placeholder per HIGH 3; 06-06 STEP L psql + STEP M JSON use HTML-comment sentinels with positive + negative cross-contamination assertions per BLOCKER 4; 06-06 CHECKPOINT 2 budget breakdown clarified as $0.62 Task 2 preview + $0.62 Task 4 staging + $0 approvals = $1.24 per MEDIUM 2; 06-06 Task 4 re-authored in clean STEP A→B→C→D→E→F→G–L order per LOW 1), checker iteration 1 returned VERIFICATION PASSED with goal-backward trace confirming 9 per-stream v3 bodies would render coherent chrome end-to-end (no silent fallback to Stream:Other). FINAL: 7 plans across 5 waves, commit 5453362, pushed to origin/production. Wave 1 [06-01a, 06-02] parallel (write copy markdown + Prisma additive migration). Wave 2 [06-01b copy-approved checkpoint + dict synthesis, 06-03 requireReview additive field] parallel. Wave 3 [06-04 4 new pending-review routes + /edit re-render]. Wave 4 [06-05 PendingReviewQueue component + Apollo Campaign button on /campaigns]. Wave 5 [06-06 deploy + 9-stream live verify, autonomous:false, 2 checkpoint:human-action tasks (deploy approval, live-verify approval)]. Deploy-to-live gate locked at 06-CONTEXT.md "🛑 DEPLOY-TO-LIVE REQUIRES EXPLICIT USER APPROVAL" — no scp/rsync to /var/www, no pm2 restart, no prisma migrate deploy, no /upgrade-streams-v3 against brandmonkz.com, no real Resend dispatch without typed user approval. Cost budgeted ~$1.24 Claude (live verify only); zero Claude during build. Next: /gsd:execute-phase 6 in fresh session OR continue in this session. Apology to rajesh@techcloudpro.com sent earlier today (Resend msgId 06536925-0b2e-4571-867e-149466bee316).

Prior activity: 2026-05-31 - Phase 6 KICKOFF prepped (ROADMAP entry + CONTEXT.md + handoff written + Rajesh apology sent). After quick-8 close-out, sent v3 branded body to jm@techcloudpro.com + rajesh@techcloudpro.com as proof — JM email landed with fallback content (personalized=0, $0.1241 Claude charged regardless), Rajesh email landed with fresh Centella Claude tokens (personalized=1, $0.1210). User opened the inbox and immediately flagged a STRUCTURAL messaging incoherence: the quick-8 v3 shell is NetSuite-specific (11 hardcoded NetSuite references — preheader, header chrome "1000+ Implementations", "NetSuite Practice" metric, "We're a senior NetSuite and AI team" intro, "A note on NetSuite Next 2026" inline callout, "01 NetSuite Practice / 02 ArthaBuild AI Your NetSuite copilot" service props, footer reprise, ArthaBuild banner, ARIA blurb). Phase 5 personalizes per-Stream (Cybersecurity/NetSuite/RPA/Web3/Data/Cloud/Mobile/AI-ML/Other), so a Cybersecurity recipient sees Cybersecurity AI paragraphs wrapped in NetSuite chrome — incoherent message. User directives: "ensure the intent is perfectly [h]onored" + "we need to have a provision for rajesh to ensure the message is right" — locks in 3-thread Phase 6. Apology sent to rajesh@techcloudpro.com from Sara via Resend (msgId 06536925-0b2e-4571-867e-149466bee316, Sun, 31 May 2026 03:27:29 GMT, monthly quota remaining 574) — Draft A wording (references prior subject, names NetSuite/Cybersecurity mismatch, sets expectation that clean version follows). Phase 6 entry added to ROADMAP.md (REQ-060 9 per-stream v3 bodies + REQ-061 status='pending_review' enum + REQ-062 requireReview body param additive + REQ-063 3 new pending-review routes + REQ-064 Pending Review tab + REQ-065 Apollo Campaign button + REQ-066 9-stream live verification with Rajesh approval). CONTEXT.md written at .planning/phases/06-stream-coherent-templates-and-review-queue/06-CONTEXT.md (~9.5KB, full design: per-stream copy direction table, 3 backend route specs, frontend tab + button design, firewall list, planner-MUST-NOT list, deploy gotchas). Handoff doc at ~/.claude/handoffs/2026-05-31-brandmonkz-phase-6-kickoff.md with resume command. NOT YET DONE: Phase 6 plan files. Next action: fresh Claude session in production-crm cwd runs /gsd:plan-phase 6 — CONTEXT.md is authoritative locked design intent, planner should produce 4-6 plans in 2-3 waves. Session total spend: ~$0.37 Claude. Phase 4 firewall + Phase 5 send-path firewall to be preserved through Phase 6.

Prior activity: 2026-05-31 - Quick task 8 (Port TCP v6 branded email shell into Phase 5 stream templates as v3 body) COMPLETE & deployed. Two atomic commits on `production` (`59a108e` feat: STREAM_TEMPLATE_V3_BODY + upgradeStreamTemplatesToV3 helper + POST /api/email-templates/upgrade-streams-v3 endpoint; `95edf69` docs: V3-PREVIEW.json evidence from Ricardo Deben re-smoke). Backend-only — frontend dist untouched. Deployed to EC2 `/var/www/crm-backend/dist/` via tarball+scp + locked pm2 env-reload-restart (restart 10→11, online, zero FATAL). 9 prod Stream:* rows for smoke user upgraded from v2 (~150 chars) to v3 (21,646 chars branded shell) — idempotent via `1000+ Implementations` sentinel. Live-verified 4/4 gates: /upgrade-streams-v3 call 1 returned `{upgraded:[9 names], alreadyV3:[], total:9}`; call 2 returned `{upgraded:[], alreadyV3:[9 names], total:9}` (idempotency proven); psql 7-boolean row on `cmpsxxybw0007h652m8c1fjsj` returned `t|t|t|t|t|t|t` (body>5KB, has Sara, has brand chrome, has {{intentHook}} placeholder, has mailto unsubscribe, video stripped, tracking stripped); Ricardo Deben Case A preview (previewOnly=true, NO send) returned HTTP 200 with all 14 hard-acceptance assertions PASS — branded 21,646-char body rendered with "Hi Ricardo," substituted + 4 AI paragraphs about Centella Health Tech (Med-Lab rebrand / Southeast expansion / HIPAA / connected-device security) + Sara signature + mailto:sara@techcloudpro.com unsubscribe; zero {{intentHook}}/{{companyContext}}/{{painPoint}}/{{cta}}/{{firstName}}/{{videoUrl}} leaked; "Watch with sound" CTA stripped (video block removed during port). Total cost ~$0.123806 Claude+web_search (33,422 input tokens + 236 output + 2 web_searches), $0 Resend (preview only). Phase 4 firewall PRESERVED byte-for-byte across both quick-8 commits: `git diff phase-04-baseline..HEAD -- campaigns.ts awsSES.ts | wc -l = 0`. Phase 5 send-path firewall PRESERVED: `apollo.ts` 0-touch in HEAD~2..HEAD. UI firewall PRESERVED: 0 touches across NetSuiteCampaignWizard + ContactList + ApolloPage + ApolloSearchForm. Files modified HEAD~2..HEAD: exactly 3 (stream-templates.ts + emailTemplates.ts + V3-PREVIEW.json). Deviation: Rule-3 inline psql DATABASE_URL `?schema=public` strip via `sed -E 's/[?&]schema=[^&]+//g'` BEFORE passing to psql 14 (no source change, no commit — verification command scope only). Closes the Phase 5 SUMMARY's deferred "branded TCP v6 template integration" user-flagged work item — Sara is now the correct sender AND the body is now the branded v6 shell with AI tokens inside the orange-tinted callout. SUMMARY: `.planning/quick/8-port-tcp-v6-branded-email-design-into-ph/8-SUMMARY.md`. Evidence: `V3-PREVIEW.json` (23,586 bytes). Push fast-forward `df2bd95..95edf69`.

Prior activity: 2026-05-31 - Phase 05 COMPLETE — AI-personalized Apollo campaign verified end-to-end live on production EC2. 4 plans landed across the phase: Wave 1 (Plan 05-01 — personalized_email_sends Prisma table + 9 stream templates upgraded to v2 with {{intentHook}}/{{companyContext}}/{{painPoint}}/{{cta}} placeholders + idempotent upgrade endpoint), Wave 2 (Plan 05-02 — backend personalizeContactWithClaude helper with EXACTLY 5 fallback warnings + web_search_20250305 + POST /api/apollo/send-personalized-campaign route with 250ms pacing + 50-contact hard gate + previewOnly slice(0,1) + testRecipient opt-in + 7-field cost telemetry envelope + frontend apolloApi.sendPersonalizedCampaign client with 600s axios timeout + 5 typed interfaces), Wave 3 (Plan 05-03 — NetSuiteCampaignWizard 4→5 step refactor with new Step 3 AI Personalize first-contact preview gate + Step 4 Review reusing previewResult.audit[0] + Step 5 Done with cost summary + Re-generate ✨ button + Send Another full-state-reset), Wave 4 (Plan 05-04 — deploy + 4-case smoke + close ceremony). Plan 05-04 Task 0 created stable firewall anchor `phase-04-baseline` tag at commit 0175cc3 + Anthropic pre-flight probe returned type=message for claude-sonnet-4-6 confirming workspace key+model access. Task 1 pushed 9 commits b19feec..2e1f402 to origin/production, deployed backend dist to /var/www/crm-backend/dist/ + frontend dist to /var/www/brandmonkz/ (with documented ~5s Vite-broken-window tradeoff), applied Prisma migration 20260531120000_phase05_personalized_email_send (verified via authoritative `_prisma_migrations.finished_at IS NOT NULL` psql assertion — survives the 2 pre-existing failed migrations' P3018 noise), upgraded 9 stream templates to v2 via POST /api/email-templates/upgrade-streams-v2 endpoint (verified via psql that smoke template cmpsxxybw0007h652m8c1fjsj.htmlContent contains literal {{intentHook}}), pm2 restarted (restart count 10). Task 2 ran 4-case live smoke against Ricardo Deben (cmpsz0d3q000350mxrlau3sg5) at Centella Health Tech: Case A previewOnly returned HTTP 200 with TIGHT acceptance PASSED (personalized=1, personalizeFailures=0, intentHook non-null, body contains "Ricardo", zero {{intentHook}} raw literal — AI claim falsifiable AND falsified i.e. proven); Case B live send to testRecipient=jm@techcloudpro.com returned HTTP 200 sent=1, Resend msgId e95edd09-2029-4101-846f-a8c36c01954a; Case C idempotency send returned HTTP 200 sent=1 new auditId cmpt5hch30005fw2pbr7zimu8 Resend msgId 1ab6897c-ddad-44a8-88d8-99494a46d8d5; Case D hard gate returned HTTP 400 large_batch_requires_confirmation with $0 spend. Total smoke cost ~$0.374 Claude+web_search; $0 Resend (free tier). 2 emails delivered to jm@techcloudpro.com from Sara <sara@techcloudpro.com> with Subject "Security engineers — bench available" and 4 AI paragraphs referencing Centella Health Tech's Med-Lab rebrand + Southeast healthtech expansion + HIPAA + connected-device security footprint (genuinely personalized intent signals, NOT fallback strings). User-acceptance checkpoint APPROVED implicitly via progression to next request (branded TCP v6 template integration). Phase 4 firewall PRESERVED byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l` = 0 across the entire Phase 5 commit range. Task 3 close ceremony wrote .planning/phases/05-ai-personalized-apollo-campaign/05-04-SUMMARY.md (plan-level) and 05-SUMMARY.md (phase-level aggregate covering all 4 plans + cost + firewall + 12 locked decisions + 5-fallback contract + deferred items), updated this STATE.md with COMPLETE paragraph, updated ROADMAP.md marking 05-04 [x] + "Phase 5 COMPLETE 2026-05-31" marker after the plan list. Branch parity: production at origin/production at parity post-push (0 commits ahead). Author jm@techcloudpro.com. Immediate next work item (user-flagged, NOT a Phase 5 gap): branded TCP v6 template integration — wrap the 4 AI tokens in the v6 HTML shell that Sara's TCP retargeting pipeline already uses per memory `tcp_v6_sender_rotated` (Sara already correct sender; integration is template-substitution exercise: STREAM_TEMPLATE_V2_BODY → v6 shell with same 4 placeholders). 5 requirements closed: REQ-050, REQ-051, REQ-052, REQ-053, REQ-054.

Prior activity: 2026-05-31 - Phase 5 Plan 03 (NetSuiteCampaignWizard 4→5 step refactor with AI Personalize Step 3 + Review Step 4 + Done Step 5) COMPLETE. Two atomic commits on `production` (8344905 feat: 386 insertions / 34 deletions — state machine extended to `useState<1 | 2 | 3 | 4 | 5>(1)`, step indicator labels extended to ['Audience', 'Email', 'AI Personalize', 'Review', 'Done'], previewing/previewError/previewResult/confirmedLargeBatch state added + LARGE_BATCH_THRESHOLD=50 const + firstContactId=Array.from(selectedIds)[0]||null derivation, sendResult state added to replace sentCount/failedCount, fetchPreview() callback calling apolloApi.sendPersonalizedCampaign({contactIds:[firstContactId], templateId, suggestedStream, previewOnly:true}), useEffect with deps [step, firstContactId, templateId] for cached auto-fetch on Step 3 entry, Back-button cache invalidation clearing previewResult+previewError before setStep(2), full Step 3 JSX with inbox-card preview + AI token attribution panel + N>50 confirmation checkbox + Re-generate ✨ button, handleSend body rewired to call apolloApi.sendPersonalizedCampaign({contactIds:Array.from(selectedIds), templateId, suggestedStream, confirmedLargeBatch:N>50?true:undefined}) — old apolloApi.sendCampaign reference fully removed in this commit; 46151a4 feat: 180 insertions / 128 deletions — Step 4 (Review) JSX rewritten to re-use previewResult.audit[0] for the inbox-card preview with "Sending to N contact(s)" summary + Send button calling handleSend live, Step 5 (Done) JSX rewritten to render sent/failed/personalized/personalizeFailures/totalCostUSD + Claude token usage line + Resend cost + collapsible failureDetails, dead validationWarnings/bodyPlain/sendBlocked block removed, "Send another campaign" button resets ALL Phase 5 state). All 27 verify-block greps pass. Frontend tsc --noEmit entirely clean. Phase 4 + Wave 2 firewall holds byte-for-byte (HEAD~2 diff over backend/src/routes/apollo.ts, backend/src/routes/campaigns.ts, backend/src/services/awsSES.ts, backend/prisma/schema.prisma, frontend/src/pages/Contacts/ContactList.tsx, frontend/src/pages/Apollo/ApolloPage.tsx, frontend/src/components/ApolloSearchForm.tsx = 0 lines). Task 0 grep verified canonical names matched plan-time pin exactly: step (line 65), selectedIds:Set<string> (line 69), templateId:string|null (line 75), sending (line 81), sendError (line 82), importedContactIds prop (line 34), suggestedStream prop (line 35) — zero file drift. importedContactIds[0] forbidden pattern verified absent. handleSend body rewrite folded into Task 1's commit per Rule-3 (avoids intermediate state where commit references both old + new send paths). Plan-corrections: Back-button cache invalidation written as multi-line arrow body (3 statements) not the plan's pinned single-line — semantically identical, both commits/tsc/runtime unchanged. Author jm@techcloudpro.com. Plan 05-04 (deploy + 4-case smoke against Ricardo Deben) unblocked.

Prior activity: 2026-05-31 - Phase 5 Plan 02 (personalizeContactWithClaude helper + send-personalized-campaign route + frontend client) COMPLETE. Two atomic commits on `production` (908e631 feat: 469 backend insertions — Phase 5 types + personalizeContactWithClaude helper with EXACTLY 5 distinct warning strings + POST /api/apollo/send-personalized-campaign route with audit-before/after + previewOnly slice(0,1) + 50-contact confirmedLargeBatch gate + 250ms pacing + Prisma.InputJsonValue/JsonNull cast + 7-field cost telemetry envelope; 35bbc58 feat: 64 frontend insertions — 5 typed interfaces + apolloApi.sendPersonalizedCampaign with 600s axios timeout). Non-object JSON branch FOLDED into 'Claude returned malformed JSON' via manual throw inside try/catch — verified by sed-scoped grep within personalizeContactWithClaude returns EXACTLY 5 distinct warnings, 0 'non-object JSON' emitted to caller. Phase 4 firewall holds byte-for-byte (HEAD~2..HEAD diff over campaigns.ts/awsSES.ts/NetSuiteCampaignWizard.tsx/ApolloSearchForm.tsx/ApolloPage.tsx/ContactList.tsx = 0 lines). Existing apollo.ts routes (/import line 523, /normalize-filters line 721, /send-campaign line 741) unchanged; new /send-personalized-campaign at line 875. Backend tsc clean on apollo.ts after `prisma generate --schema=prisma/schema.prisma` refreshed local Prisma client to v5.22.0 (resolved pre-existing Phase 4 + new 05-01 errors). Frontend tsc entirely clean. Plan-corrections during execution: auth idiom uses req.user?.id || req.user?.sub matching adjacent /send-campaign route (NOT req.user?.userId as plan suggested) — same Rule-3 deviation re-applied from Wave 1. Local Prisma client regeneration was a tooling-only fix (no source change). Author jm@techcloudpro.com. Plan 05-03 (wizard 4→5 step refactor) unblocked.

Prior activity: 2026-05-31 - Phase 5 Plan 01 (PersonalizedEmailSend schema + Stream template v2 upgrade) COMPLETE. Two atomic commits on `production` (b19feec feat: PersonalizedEmailSend model + 20260531120000 migration; 820fd4a feat: STREAM_TEMPLATE_V2_BODY + 9-stream STREAM_FALLBACKS + idempotent upgradeStreamTemplatesToV2 + POST /api/email-templates/upgrade-streams-v2 endpoint). schema.prisma model count 53 → 54 (additive only, prisma 5.4.2 validate clean). Migration .sql force-added past backend/.gitignore (CREATE TABLE personalized_email_sends + 5 indexes + 3 FKs: contacts CASCADE, email_templates RESTRICT, users CASCADE). Stream template upgrade endpoint idempotent via `htmlContent.includes('{{intentHook}}')` sentinel — first call updates 9 rows, second returns alreadyV2 list of all 9. Phase 4 firewall clean (git diff cc964ad..HEAD over campaigns.ts/awsSES.ts = 0 lines). Plan-corrections during execution: actual file is emailTemplates.ts (camelCase) not email-templates.ts; auth idiom uses req.user?.id (matches existing seed-streams handler) not req.user?.userId. Migration .sql committed but NOT yet applied — deferred to Plan 05-04 deploy. No EC2 changes this plan. Author jm@techcloudpro.com. Plan 05-02 (AI helper + send-personalized-campaign route) unblocked.

Prior activity: 2026-05-30 - Quick task 7 (Apollo Claude auto-normalize on Search + form UX upgrades) COMPLETE & deployed. Backend `normalizeFiltersWithClaude()` helper using claude-sonnet-4-6 (temp 0, max_tokens 1000, 8s `Promise.race` timeout) — auto-corrects user mistakes like "Saas Companies in Irvine" in keyword tags BEFORE Apollo HTTP call (extracts SaaS as discrete tag, moves Irvine to personLocations). New `POST /api/apollo/normalize-filters` standalone route. `autoNormalize` default-true injection in `/api/apollo/import` — response now appends `corrections` + optional `warning`. Frontend: `apolloApi.normalize()` method + extended `ApolloImportResponse` types; `ApolloSearchForm` got "📋 Common ICP examples" `<details>` collapsible (4 presets) + concrete placeholders + per-field ✓/✗ green/red hints on all 3 CSV inputs; `ApolloPage` results panel renders "🤖 Claude refined your filters" indigo block when corrections non-empty + yellow "⚠ Claude unavailable" block when warning set. 2 commits on `production` (`d0a3708` feat + `17c2ceb` Rule-1 deviation fix bumping timeout 3s→8s after Case A's first run timed out at 3.3s). Deployed via tarball+scp + locked pm2 env-reload-restart recipe. Live-verified 3/3 cases: Case A `/normalize-filters` dirty input → 200 + 1 correction (SaaS extracted, Irvine moved); Case B `/import` dirty → 200 in 8.79s + 3 imported + 1 correction; Case C `/import` clean → 200 + 0 corrections (no false positives, 2 imported). pm2 crm-backend online (restart 9), zero new P2002. Phase-4 firewall clean (HEAD~2..HEAD over campaigns.ts/awsSES.ts/NetSuiteCampaignWizard.tsx/ContactList.tsx/schema.prisma = 0 lines). Push fast-forward `cf74c02..17c2ceb`. ANTHROPIC_API_KEY on EC2 = TCP workspace key (108 chars).

Prior activity: 2026-05-30 - Quick task 6 (Apollo 120s per-call timeout + Company P2002 guard) COMPLETE & deployed. Frontend per-call `{ timeout: 120000 }` override on `apolloApi.import` + `apolloApi.sendCampaign` (default 10s preserved for the other 100+ API methods). Backend 3-case Company find-or-update-or-create ladder (findUnique apolloOrgId → findUnique domain → update | create) eliminating the P2002 companies_domain_key crash at apollo.ts:193. Single atomic commit `861ed00` on `production` (`jm@techcloudpro.com / jeet-avatar`). Deployed to EC2 (`/var/www/crm-backend/dist/` + `/var/www/brandmonkz/`) via tarball+scp + pm2 env-reload-restart. Live-verified: 2 consecutive `POST /api/apollo/import` runs returned HTTP 200 in ~9s each, 4 imported + 1 dedupe-skipped per run, zero NEW P2002 in pm2 logs (the 2 existing P2002 lines reference old `apollo.ts:193` from pre-deploy). Smoke #2 almost certainly exercised case (b) of the ladder — Myers-Holum domain already in DB from smoke #1, all 4 new people from same org → UPDATE path, NOT create. State correction confirmed: Apollo key on EC2 is LIVE (HTTP 200, not 503). Phase-4 firewall verified clean (0 touches to campaigns/awsSES/ApolloPage/NetSuiteCampaignWizard/schema.prisma). Push fast-forward `42cf8ca..861ed00`.

Prior activity: 2026-05-30 - Quick task 5 (Wire ContactList ?source=apollo filter end-to-end) COMPLETE & deployed. Backend `where.source` filter + frontend API client signature + ContactList URL-driven sourceFilter state + dismissible "Source: Apollo" chip + Apollo/stream badges on both row render sites. Single atomic commit `8b99d33` on `production`. Deployed to EC2 (`/var/www/crm-backend/dist/routes/contacts.js` + `/var/www/brandmonkz/`) via tarball+scp + pm2 restart w/ env reload. Live-verified: `GET https://brandmonkz.com/api/contacts?source=apollo` returns exactly 1 row = `cmpsz0d3q000350mxrlau3sg5` (Ricardo Deben / Cybersecurity stream) with zero non-apollo leak — closes the Phase-4 visibility gap that the 04-04 SUMMARY's locked decision had assumed was already in place. Phase-4 firewall verified clean (zero touches to campaigns/awsSES/ApolloPage/NetSuiteCampaignWizard/schema.prisma). Frontend deploy path confirmed = `/var/www/brandmonkz/` (TBD closed). Push fast-forward `6d1aa88..8b99d33`.

## Current Phase
Phase 06: Stream-Coherent Templates and Review Queue — IN PROGRESS (Wave 4 of 5 COMPLETE; 6/7 plans complete: 06-01a + 06-01b + 06-02 + 06-03 + 06-04 + 06-05; Wave 5 = Plan 06-06 BLOCKED on explicit user deploy approval per CONTEXT.md hard rule)

## Current Position
- Phase: 06-stream-coherent-templates-and-review-queue — IN PROGRESS
- Plan: 06-05 COMPLETE (Apollo Campaign button on /campaigns header BEFORE Create Campaign + Pending Review tab toggle + PendingReviewQueue inbox-card preview component with per-row Approve/Edit/Reject + bulk Approve all + Edit token modal that patches local item with server-re-rendered renderedBody — Plan 06-04 MEDIUM 1 fix flows through; apolloApi.unsentContacts + apolloApi.pendingReview.{list,approve,reject,edit} typed bindings; VALID_STREAMS_FRONTEND inline 9-element Set guard mirroring backend apollo.ts:73-76; HIGH 3 fix: static emailTemplatesApi.findByCategory import — NO _notRealMethod_ placeholder, NO dynamic await import inside click handler; BLOCKER 1+3 fix: 9 real prod streams only — zero RPA/Web3/AI-ML; BLOCKER 2/3 fix: NO-space 'Stream:<name>' template lookup format; DOMPurify.sanitize on renderedBody preview matching NetSuiteCampaignWizard.tsx:621 pattern; empty-state copy verbatim per CONTEXT.md). Commit `9bb3311` pushed to origin/production.
- Active branch: `production` (at origin/production parity post-push; 0 commits ahead)
- Rollback target: `backup/pre-3.1-production-state` (local + origin)
- Next: Wave 5 = Plan 06-06 (deploy gate with `autonomous: false` + 2 checkpoint:human-action approvals (deploy approval + live-verify approval) + 9-stream live verify with Rajesh approval at least 1 row). **BLOCKED** until user explicitly types `deploy` per CONTEXT.md "🛑 DEPLOY-TO-LIVE REQUIRES EXPLICIT USER APPROVAL" hard rule — no scp/rsync to /var/www, no pm2 restart, no prisma migrate deploy, no /upgrade-streams-v3 against brandmonkz.com, no real Resend dispatch.

Prior phase: Phase 05 COMPLETE 2026-05-31 (4/4 plans done; AI-personalized Apollo campaign live with Sara as sender; Phase 4 firewall held byte-for-byte).

Prior phase: Phase 04 COMPLETE 2026-05-30 (6/6 plans done; Resend send-half verified live; Apollo IMPORT deferred to Phase 4.5 awaiting external Apollo key refresh).

## Decisions Made (Phase 06 additions — Plan 06-05)
- Pre-flight `emailTemplatesApi.findByCategory` shape check (HIGH 3 fix) PASSED: `grep -c "findByCategory" frontend/src/services/api.ts = 1` confirmed method exists at api.ts:246 with signature `(category: string) => Promise<EmailTemplate | null>` returning first match by category — no shape mismatch, no method invention needed
- Single atomic commit covering all 3 frontend files (Task 1 + Task 2 + Task 3 squashed per plan STEP G instruction — frontend UI surface is one logical change; atomicity here matches Plan 06-04's backend-routes atomicity precedent)
- Module-scope `VALID_STREAMS_FRONTEND` const (NOT useMemo inside component) — it's a runtime constant with zero React dependencies, never re-evaluated, declared once at module load. Mirrors backend `apollo.ts:73-76` VALID_STREAMS exactly: NetSuite, AI/ML, Cloud/DevOps, Cybersecurity, Data/Analytics, Mobile, Enterprise/ERP, Staffing/HR, Other
- Rogue stream values silently routed to 'Other' bucket (with `console.warn`) rather than throwing — preserves Apollo Campaign batch flow when a contact has an out-of-date stream value (e.g., row predates the 9-stream taxonomy migration). Throwing would abort the entire 200-contact batch over one row
- Defensive post-grouping assertion `throw new Error("Internal error: byStream contains invalid stream keys: ...")` if any byStream key escapes VALID_STREAMS_FRONTEND — belt + suspenders. The per-contact filter should make this unreachable, but defends against logic bugs that mutate the bucket map after assignment
- Per-stream sequential dispatch (one `apolloApi.sendPersonalizedCampaign` call per stream group) rather than one bulk call — Plan 06-03 backend requires single `templateId` per call (route validates `suggestedStream ∈ VALID_STREAMS` at apollo.ts:917 and uses the template for all contacts in that one call). Sequential is preferred over `Promise.all` because Claude's per-contact personalize is rate-bound by the 250ms backend pacing — parallel calls would over-pipeline and trigger rate limits
- Apollo Campaign button orange-to-amber gradient + `RocketLaunchIcon` (distinct from Create Campaign's indigo + `PlusIcon`) — visually obvious new entry point, doesn't blend into existing chrome. Label switches to "Staging N/M streams…" with live progress during dispatch
- Apollo Campaign button positioned BEFORE Create Campaign in the same `<div className="flex items-center gap-3">` flex row — line-number proof: Apollo label first hit = line 28 (comment block) and actual button label hit = line 259, Create label hit = line 395, `$APOLLO_LINE < $CREATE_LINE` verified
- HARD_CAP=200 + LARGE_CONFIRM=50 thresholds: hard cap shown when total > 200 with explicit "process first 200" wording, large-batch confirmation shown when total > 50 with Claude cost estimate (`$${(total * 0.069).toFixed(2)}`). Per-group `confirmedLargeBatch: group.length > 50` mirrors Plan 06-03 backend gate which validates per-call (not per-batch) at the 50-contact boundary
- Edit modal calls `apolloApi.pendingReview.edit({aiTokens})` only (NOT `renderedBody`) — relies on Plan 06-04 MEDIUM 1 fix server-side re-render. Local item patched with response.renderedBody so preview refreshes immediately without a second fetch. Manual renderedBody overrides remain possible by passing the field explicitly but the current UI doesn't expose that path (Rajesh edits tokens, server re-renders)
- Inbox-card preview body container `max-h-[400px] overflow-y-auto` — the 21KB+ branded HTML body would otherwise dominate the viewport. Scroll container respects per-card visual hierarchy and keeps the action buttons visible
- Pending Review queue uses optimistic remove on Approve/Reject (filter local items array + decrement total) — fetchList() reset only after bulk approve completes (or partial-failure stop). Snappier UX vs full refetch per click
- Empty-state copy verbatim per CONTEXT.md line 111: heading "No pending emails." + sub-line "Click \"Apollo Campaign\" above to generate the next batch." Stays consistent with the planner contract; future copy revisions go through CONTEXT.md not component-local strings
- Default tab UNCHANGED — `view: 'campaigns'` is the initial state. The existing campaigns list rendering is wrapped in `{view === 'campaigns' && (...)}` so user sees the same `/campaigns` page they always did unless they click the new "Pending Review" tab
- Existing status filter chips (all/draft/scheduled/active/completed/paused) gated on `view === 'campaigns'` — they're meaningless in the Pending Review context (rows have status='pending_review' by construction). PendingReviewQueue owns its own optional `streamFilter` prop for future stream-narrowing (currently unset by parent)
- DOMPurify imported via existing `dompurify@^3.3.3` package dep (frontend/package.json) — same package used by NetSuiteCampaignWizard.tsx:11. No new dependency. `dangerouslySetInnerHTML={{__html: DOMPurify.sanitize(item.renderedBody)}}` pattern mirrors `NetSuiteCampaignWizard.tsx:621` byte-for-byte
- All Approve / Reject / Edit / Bulk Approve calls go through `apolloApi.pendingReview.*` typed methods — zero direct fetch / axios / apiClient calls in PendingReviewQueue.tsx (`grep -c "axios\|apiClient" = 0`). Auth headers + base URL inherited from `apiClient` interceptors
- NetSuiteCampaignWizard.tsx 0-touch verified both working-tree AND HEAD~1..HEAD: 0 lines — the wizard continues to use Phase 5's immediate-send path on /apollo unchanged. The new Pending Review flow on /campaigns is a parallel surface, NOT a wizard refactor
- Frontend untouched in backend prefixes; backend untouched in frontend prefixes; schema/migrations untouched — single atomic commit modifies exactly 3 files, all under `frontend/src/`
- Phase 4 SES firewall HELD byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0` post-commit
- Phase 5 send-path firewall HELD byte-for-byte: `git diff HEAD~1..HEAD -- backend/src/routes/apollo.ts | wc -l = 0` post-commit
- Single atomic commit `9bb3311` (3 files: api.ts + PendingReviewQueue.tsx + CampaignsPage.tsx), 678 insertions + 18 deletions, author `jeet-avatar <jm@techcloudpro.com>`; pushed fast-forward `d60d845..9bb3311` to origin/production
- tsc --noEmit clean on all 3 modified files: `cd frontend && npx tsc --noEmit -p . 2>&1 | grep -E "(CampaignsPage|PendingReviewQueue|services/api)"` returns 0 lines
- 5 new typed apolloApi methods + 7 new interfaces + 2 new optional fields on existing Phase 5 types — all additive, zero existing methods modified, zero existing types modified beyond field additions
- Closes REQ-064 (Pending Review tab) and REQ-065 (Apollo Campaign button)

## Decisions Made (Phase 06 additions — Plan 06-04)
- Pre-flight schema check (HIGH 1 fix) PASSED before any route code: `grep -c "renderedBody\s*String" backend/prisma/schema.prisma = 1` (line 2357) AND `grep -c "fromEmail\s*String" backend/prisma/schema.prisma = 5` (line 2353 PersonalizedEmailSend.fromEmail among other models). Phase 5 audit columns confirmed present; /approve's `row.renderedBody` + `row.fromEmail` reads compile cleanly
- /approve re-uses persisted renderedBody — NO fresh personalizeContactWithClaude call. Cost guarantee = 0 Claude tokens + 1 Resend send. Personalization spend (~$0.069/contact) was already paid when row was queued by Plan 06-03's requireReview:true path. Verified via `grep -A 30 "router\.post('/pending-review/:id/approve'" | grep -c "personalizeContactWithClaude" = 0`
- /edit re-renders renderedBody SERVER-SIDE when aiTokens provided AND renderedBody NOT explicitly provided (MEDIUM 1 fix — closes stale-preview bug from earlier plan iteration where saved tokens didn't update the body Rajesh sees). Re-loads `EmailTemplate.findFirst({id: row.templateId, userId, select: {htmlContent: true}})`, builds 7-placeholder map `{firstName, companyName, title, intentHook, companyContext, painPoint, cta}`, runs `body.replace(new RegExp(\`\\{\\{${key}\\}\\}\`, 'g'), safeValue)` for each key — IDENTICAL semantics to /send-personalized-campaign substitution loop at apollo.ts:1002-1006 (same regex pattern, same global flag, same 7-placeholder set locked at Phase 5 Plan 05-02 SUMMARY)
- /edit: explicit `renderedBody` override wins when both `aiTokens` AND `renderedBody` provided — manual edit by Rajesh (e.g., fixing a Claude typo) is preserved, no re-render
- /edit: 409 `template_missing_for_rerender` returned when row's EmailTemplate was deleted between queueing + editing — no silent fallback, explicit failure mode with workaround hint in detail message (user can still edit by passing renderedBody directly)
- /reject re-uses EXISTING `aiWarning` column for optional reason — additive-only Phase 6 schema strategy preserved (no new column needed). CONTEXT.md route table line 102 treats reason as optional metadata, not a primary field. When reason not provided, preserves existing aiWarning value (e.g., the Phase 5 "Claude timed out" warning that may have triggered the review queue in the first place)
- All 3 mutation routes (approve, reject, edit) gate on `status='pending_review'` via SINGLE `findFirst({where: {id, userId, status: 'pending_review'}})` — atomic intent prevents racing double-approve, prevents edit of already-sent rows, AND catches both missing-row AND wrong-status in one query (404 'pending_review_row_not_found' covers both)
- All 5 routes enforce userId tenant isolation via Prisma WHERE clause (defense in depth — `router.use(authenticate)` at line 82 already gates the prefix, but explicit `userId` in WHERE prevents IDOR even if auth middleware ever regresses). Verified: 13 userId occurrences within first 12 lines of each new route block
- 502 on Resend dispatch failure DELIBERATELY LEAVES row in `status='pending_review'` so user can retry approving without losing the queued state — matches must_have truth from CONTEXT.md route table
- Pagination guards: `Math.min(Math.max(limitRaw || 500, 1), 1000)` for /unsent-contacts limit, `Math.min(Math.max(pageSizeRaw || 20, 1), 100)` for /pending-review pageSize — both clamp to documented ceilings without throwing
- /pending-review uses `prisma.$transaction([findMany, count])` for atomic paginated read — `total` is consistent with returned `items` even under concurrent inserts
- Single atomic commit covering Task 1 (2 GET routes) + Task 2 (3 POST routes) per plan STEP D-E instruction — one logical "pending-review CRUD routes" change, no helper extracted (skip pattern is inline, no reuse value across routes)
- `APOLLO_FROM_EMAIL` + `RESEND_COST_PER_SEND` constants RE-USED — no new top-level constants introduced, defense against drift between routes
- `personalizeContactWithClaude` helper UNTOUCHED — 5/5 distinct warning strings still emit (Phase 5 fallback contract preserved byte-for-byte; verified via sed-scoped grep within helper body lines 222-322 returning 5/5 hits)
- Frontend `apolloApi.sendPersonalizedCampaign` interface NOT updated by this plan — the 5 new routes get their frontend bindings via NEW apolloApi methods (unsentContacts, pendingReview.list/approve/reject/edit) added by Plan 06-05. NetSuiteCampaignWizard call site stays unchanged
- Phase 4 SES firewall HELD byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`
- NetSuiteCampaignWizard.tsx 0-touch in HEAD~1..HEAD: 0 lines changed — wizard continues to use Phase 5's Step 4 live send path unchanged
- Frontend untouched, schema untouched, migrations untouched in commit — only backend/src/routes/apollo.ts modified (313 insertions, 0 deletions)
- Single commit `d8b3350` by `jeet-avatar <jm@techcloudpro.com>` pushed fast-forward `44b4eac..d8b3350` to origin/production; no deviations from plan (no Rule-1/2/3/4 triggered)

## Decisions Made (Phase 06 additions — Plan 06-01b)
- User approval message captured VERBATIM per CONTEXT.md citation rule: "lets call it approved - and send email to jm@techcloudpro.com" — first clause interpreted as `copy-approved` literal token required by Task 1's checkpoint:human-action gate; second clause handled separately by orchestrator after this plan's commit lands
- Dict keys use NO-SPACE 'Stream:<bare>' format (NOT 'Stream: <bare>' with space) — matches the EXACT prod DB email_templates.category writes from `seed.category = \`Stream:${seed.stream}\`` at stream-templates.ts:102; using space would silent-fallback-to-Other on every prod row
- HTML-comment sentinel `<!-- STREAM_V3:<bareStream> -->` baked as FIRST line immediately after `<body>` is the authoritative idempotency anchor — survives future copy revisions within a stream, unique per stream by construction, can't be accidentally matched in another stream's body. Beats visible-copy-uniqueness sentinels (those couple idempotency to copy stability)
- `buildStreamV3Body(opts: StreamV3CopyOpts)` is INTERNAL (not exported) — single source of truth for shared chrome (navy header, metrics row, callouts, CTA buttons, signature, footer); future copy edits per stream go through the dict opts, NOT the function body. Keeps drift impossible by construction
- Quick-8-era single-body `STREAM_TEMPLATE_V3_BODY` const DELETED entirely (not kept as deprecated alias) — quick-8 v3 was only sent to JM + Rajesh (apology already sent 2026-05-31 via Resend msgId `06536925-0b2e-4571-867e-149466bee316`); no paying-customer rows used it, so per CONTEXT.md preference we treat it as a draft revision and overwrite in place
- Only Stream:NetSuite preserves the secondary "AI banner" callout block (`<!-- AI banner -->` containing 'A note on NetSuite Next 2026' + SuiteCloud AI / Predictive Planning / AI Workspaces / ArthaBuild AI copy) — the other 8 streams pass noteCalloutTitle:'OMIT' which drops the entire gradient-blue block via conditional `${secondaryCallout}` interpolation. Per CONTEXT.md "What gets dropped from non-NetSuite v3 variants" rule
- Service props 03 + 04 are SHARED BYTE-FOR-BYTE across all 9 entries ('Dedicated AI Consulting' Custom RAG/agents/anti-hallucination + 'Transparent staffing. $1 per contract.' Same fee for candidates and companies / No hidden cuts / No 15-20% markup) — only props 01 + 02 carry per-stream copy via opts.servicePropTitle1/Body1 + opts.servicePropTitle2/Body2
- Shared visual chrome preserved byte-for-byte across all 9: <head> mso fallbacks + mobile @media, top accent bar gradient, navy `#0F172A` header with TechCloudPro · 1000+ Implementations brand-wide tagline, 4-cell metrics row (1000+/Implementations + Since 2015/<metricLabel> + 94%/Faster Close + $1/contract/Staffing Fee), orange `#FFF7ED` primary AI callout containing 4 <p> tags ({{intentHook}}/{{companyContext}}/{{painPoint}}/{{cta}}), 30-day guarantee block, 4 CTA buttons (Call ARIA + Call a Human + Visit TechCloudPro + Visit ArthaBuild AI), Sara signature, mailto:sara@techcloudpro.com unsubscribe footer
- 6-placeholder set preserved across all 9 entries: {{firstName}} + {{companyName}} + {{intentHook}} + {{companyContext}} + {{painPoint}} + {{cta}} — Phase 5 substitution loop in apollo.ts continues to work byte-for-byte; no new placeholder introduced, no existing placeholder removed
- Runtime guard at end of STEP H prints `OK 9/9` + `CROSS-CONTAM CHECK: PASS` from dist/seeds/stream-templates.js — load-bearing safety check confirms every prod Stream:* seed has matching dict entry AND each entry contains its own sentinel AND NO other stream's sentinel. Catches both silent-fallback-to-Other bug AND copy-paste-cross-contamination bug at module-load time
- `upgradeStreamTemplatesToV3` rewritten with stream-aware per-row dict lookup: derives bareStream from `row.category.slice('Stream:'.length)`, builds per-row sentinel, checks `htmlContent.includes(sentinel)` for idempotency (NOT the v3-era hardcoded `1000+ Implementations` sentinel which would fire on all 9 v3 bodies and break per-stream re-upgrade detection), falls back to `STREAM_TEMPLATE_V3_BODIES['Stream:Other']` for unknown categories (defense in depth — should never trigger given the runtime guard, but documented behavior for legacy rows that pre-date the 9-stream taxonomy)
- `/api/email-templates/upgrade-streams-v3` endpoint URL UNCHANGED (`router.post('/upgrade-streams-v3', ...)`) — only inline doc comment rewritten to reference Phase 06 plan 06-01b + per-stream dict lookup + HTML-comment sentinel + dict keys NO-SPACE format. Reusing the v3 endpoint URL (not introducing /v4) per CONTEXT.md "Do not introduce a separate v4 endpoint without strong justification" rule
- v2 exports preserved byte-for-byte: STREAM_TEMPLATE_V2_BODY (lines 146-156), STREAM_FALLBACKS dict (lines 174-224), getStreamFallbacks function, STREAM_TEMPLATE_SEEDS array, seedStreamTemplates function, upgradeStreamTemplatesToV2 function — verified via 5/5 grep counts post-commit
- Build had 8 pre-existing TypeScript errors in unrelated files (apiSubscriptions.controller.ts + analytics.routes.ts + apiSubscriptions.routes.ts + publicCheckout.routes.ts + subscriptions.ts + cronScheduler.ts + export.service.ts) — tolerated per Phase 5 SUMMARY baseline; ZERO new errors in stream-templates.ts or emailTemplates.ts (the only 2 files this plan touched); the dist/ was emitted successfully since tsc errors don't block emission with `noEmitOnError: false` default
- Phase 4 SES firewall HELD byte-for-byte: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0` post-commit
- Phase 5 send-path firewall HELD byte-for-byte: `git diff HEAD~1..HEAD -- backend/src/routes/apollo.ts | wc -l = 0` post-commit
- NetSuiteCampaignWizard.tsx 0-touch verified both working-tree AND HEAD~1..HEAD: 0 lines
- Single atomic commit `01fe98a` (2 files: stream-templates.ts + emailTemplates.ts), 254 insertions + 70 deletions, author `jeet-avatar <jm@techcloudpro.com>`; pushed fast-forward `3f8ce4c..01fe98a` to origin/production

## Decisions Made (Phase 06 additions — Plan 06-03)
- `requireReview` is HIGHEST priority status determinant — overrides `previewOnly` when both true. Realized as 3-way ternary `requireReview ? 'pending_review' : (previewOnly ? 'preview' : 'pending')` on the audit row create at line ~1015. Documented in route header comment block "Mutual exclusion: when requireReview AND previewOnly are both true, requireReview wins"
- Pacing (`PERSONALIZE_PACING_MS=250`, i.e. 4 req/sec) PRESERVED inside the new requireReview skip branch (4b) — even though no Resend dispatch happens, Claude API still gets a 250ms pause between calls. Prevents 50-contact requireReview batch from hammering Claude. Inline `await new Promise<void>((r) => setTimeout(r, PERSONALIZE_PACING_MS));` before the `continue` because `continue` would skip the post-Resend pacing line at the bottom of the loop
- Top-level response keys `queuedForReview: number` + `queueIds: string[]` placed BEFORE `cost: {}` in the response object literal. Old callers that iterate keys don't break (extras silently ignored); new callers (Plan 06-04 + 06-05) find them at predictable shallow positions
- Symmetric audit envelope shape between previewOnly + requireReview branches — both push per-contact objects with IDENTICAL field set (contactId, email, auditId, subject, renderedBody, aiTokens, aiWarning, claudeInputTokens, claudeOutputTokens, webSearchUses, status). Only `status` differs ('preview' vs 'pending_review'). Plan 06-04 routes can consume same envelope shape regardless of which queue state row is in
- No new helper function — all changes inline in the route handler. The skip branch is ~20 lines; a top-level helper would add cognitive overhead without reuse value (no other route needs this exact skip+pace pattern)
- Hard cost gate (`PERSONALIZE_BATCH_HARD_CAP=50 + confirmedLargeBatch:true`) STILL APPLIES when requireReview:true — no carve-out. Plan 06-05's Apollo Campaign button explicitly passes `confirmedLargeBatch:true` when count > 50 to clear the gate
- Single atomic commit `3e38d37` for the entire additive change — 1 file, 6 surgical edits (STEP A-F), no helper extracted, no auxiliary commits. Author `jeet-avatar <jm@techcloudpro.com>`
- `personalizeContactWithClaude` helper UNTOUCHED — 5/5 distinct warning strings still emit. Phase 5 fallback contract preserved byte-for-byte (verified by inspection — diff shows zero `^-`/`^+` lines inside lines 222-322 of the helper body)
- Frontend `apolloApi.sendPersonalizedCampaign` interface NOT updated in this plan — the optional `requireReview` field is request-side, and frontend TypeScript will need an additive interface field in Plan 06-05 (PendingReviewQueue caller). NetSuiteCampaignWizard caller stays unchanged (it never passes requireReview)
- Top-level `personalized` + `personalizeFailures` + `cost.claudeCostUSD` count Claude outcomes IDENTICALLY across all 4 states (neither flag, previewOnly, requireReview, both) — Claude is always called. Only `sent`/`failed`/`resendCostUSD`/`resendSendsCounted` vary by skip state
- Phase 4 SES firewall verified byte-clean post-commit: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`
- NetSuiteCampaignWizard.tsx 0-touch in HEAD~1..HEAD AND working-tree clean — wizard's existing call site doesn't pass requireReview → undefined → falsy → existing Resend dispatch path runs unchanged

## Decisions Made (Phase 06 additions — Plan 06-02)
- `status` column is TEXT (not Postgres ENUM) per Phase 5 schema choice — adding new string values requires zero DDL at DB layer; application code in 06-03 + 06-04 writes them freely
- 14-digit timestamp `20260601120000` (noon UTC 2026-06-01) sorts immediately after Phase 5's `20260531120000_phase05_personalized_email_send` per established Phase 03.1 + 05 precedent
- No-op SELECT statement (`SELECT '...' AS phase06_marker;`) satisfies `prisma migrate deploy`'s "must run something" requirement while writing zero bytes of data — Postgres 14+ runs it cleanly
- Migration .sql force-added past `backend/.gitignore:49` via `git add -f` — same pattern as Phase 04-01, 03.1-02, 03.1-03, 05-01
- Single atomic commit `eb6dd24` combines Task 1 schema-comment update + Task 2 migration file creation (one logical change per Task 2 STEP D-E instruction)
- Migration .sql committed but NOT applied to prod DB — `prisma migrate deploy` deferred to Plan 06-06 deploy gate per locked DEPLOY-TO-LIVE protocol; verification via `_prisma_migrations.finished_at IS NOT NULL` psql assertion (same pattern as Phase 5 Plan 05-04 HIGH #6 mitigation)
- Inline schema comment lists all 6 values in canonical order: `'pending' | 'preview' | 'sent' | 'failed' | 'pending_review' | 'rejected'` followed by `(Phase 06: pending_review + rejected added)` for git-archeology-friendliness
- Field declaration, `@default("pending")`, and indexes on `status` byte-identical to pre-Phase-6 state — only the trailing `//` comment changed; 0 model count delta
- `prisma migrate dev` NOT run locally — env has no `DATABASE_URL` and Plan 06-06 deploy gate is authoritative
- Phase 4 firewall verified clean: `git diff phase-04-baseline..HEAD -- backend/src/routes/campaigns.ts backend/src/services/awsSES.ts | wc -l = 0`
- apollo.ts NOT touched in this plan's commit — all route-level changes deferred to Plans 06-03 (requireReview field) and 06-04 (4 new pending-review routes)
- UI surfaces (NetSuiteCampaignWizard, CampaignsPage, api.ts) NOT touched — PendingReviewQueue component + Apollo Campaign button land in Plan 06-05

## Decisions Made (Phase 04 additions — Plan 04-01)
- Manual audit SQL uses lowercase @@map() table names ("contacts", "companies") instead of plan-example PascalCase — production DB uses lowercase per every prior migration.sql; PascalCase ALTER would fail
- STREAMS[] in lib exports only the 9 canonical Phase 4 streams; classifyStream() can still return broader legacy labels (Full-Stack/Web3/Product-Design/QA-Testing) — those fall through to template-fallback in Apollo wizard
- classifyStream() function body copied byte-for-byte (regex order preserved) into streamClassifier.ts — zero behavior drift between Job Leads and upcoming Apollo route
- prisma db push intentionally deferred to plan 04-06 — executor env has no DATABASE_URL per execution_context env_state
- Audit .sql file force-added (git add -f) — backend/.gitignore line 49 ignores prisma/migrations/**/*.sql; prior migration.sql files at 20251003210116_crmstartup/migration.sql were committed the same way

## Decisions Made (Phase 04 additions — Plan 04-02)
- apolloClient.ts is library-pure: does NOT read process.env. Caller (route in 04-03) injects the key. Keeps the wrapper reusable from scripts and testable in isolation.
- Apollo auth errors (401/403) surface as a named `ApolloAuthError` class so the route in 04-03 can map → HTTP 503 "Apollo key invalid or expired" without trying to parse axios error shapes.
- `enrichPerson()` returns null on ANY failure (404, 401, network) rather than throwing — lets the import loop in 04-03 simply `continue` instead of wrapping every iteration in try/catch.
- ICP filter (q_organization_industry_tag_ids whitelist or exclude-industries) deferred to Phase 4.5 — documented as an inline TODO in apolloClient.ts so future planners can grep for it.
- APOLLO_API_KEY placeholder in .env.example is intentionally empty; live key lives on EC2 only per CLAUDE.md ops policy.

## Decisions Made (Phase 04 additions — Plan 04-04)
- `apiClient.baseURL` already ends in `/api` (`api.ts:3`), so `apolloApi.import` posts to bare `/apollo/import`. Matches every other `*Api` client in the file. Including `/api` in the resource path would have double-prefixed.
- "Start campaign with these contacts →" button renders DISABLED with tooltip "Wizard handoff lands in plan 04-05". Lets `/apollo` ship in a usable state for isolated verification without coupling to wizard code that does not yet exist. Plan 04-05 just removes `disabled`/`title` and adds an `onClick` that mounts `NetSuiteCampaignWizard` with `result.contactIds` + `result.suggestedStream`.
- "See imported contacts" deep-links to `/contacts?source=apollo` to lean on the existing ContactList source filter — zero ContactList code change required (user-locked).
- Sidebar nav uses `RocketLaunchIcon` (existing heroicons dep) placed immediately after "Job Leads" because both are lead-generation sources. Active-state styling inherits the existing indigo-glow treatment for free.
- ApolloSearchForm prefills sensible CFO/Finance ICP defaults (CFO, Controller, VP Finance / United States / 100-500 emp / perPage=25). One-click search works without typing.
- `perPage` is clamped to max 25 in `handleSubmit` to enforce Apollo's per-call limit at the frontend layer (defense in depth — backend also enforces).
- Consultancy warning banner verbatim from RAJESH-HANDBOOK Section 6 / Pitfall 4 — rendered above the keyword input, not the title input (matches the actual hallucination risk surface).
- ICP industry-include / tech_uids filter UI deliberately NOT added (same Phase 4.5 deferral as plans 04-02, 04-03).
- ContactList.tsx fully untouched — `git diff frontend/src/pages/Contacts/ContactList.tsx | wc -l == 0`. Dead-code `ApolloImportModal` reference count unchanged at 2.
- Page-level error handling categorizes by HTTP status: 503 → yellow warning + operator hint, 401/403 → auth-red message, other → generic red. Pattern is reusable for other backend-key-dependent features.
- `result.contactIds` and `result.suggestedStream` are kept in component state but the IDs are NOT rendered in DOM — only the count via `result.imported`. Wizard handoff in 04-05 reads them directly from state.

## Decisions Made (Phase 04 additions — Plan 04-03)
- Resend SDK direct (not SES) for Phase 4 send path — campaigns.ts SES path stays byte-for-byte unchanged (Rajesh's BrandMonkz flow untouched). Verified via `git diff backend/src/routes/campaigns.ts | wc -l == 0`.
- From-address hardcoded to `Sara <sara@techcloudpro.com>` in apollo.ts module-level const — per-stream / configurable from-address deferred to Phase 4.5 per the locked Phase 4 scope.
- RESEND_API_KEY missing → `process.exit(1)` at module load — backend MUST NOT boot without a working send path. Pattern mirrors main_new.py JWT_SECRET RuntimeError guard.
- VALID_STREAMS allowlist Set inlined into apollo.ts validates suggestedStream body param — duplicates the canonical 9 streams from lib/streamClassifier.ts (acceptable duplication; both are small).
- Variable substitution mirrors campaigns.ts:546-559 byte-for-byte (regex `\{\{key\}\}` per known var) — kept identical so Rajesh sees the same {{firstName}}/{{companyName}} behavior in both flows.
- Per-contact try/catch with 100ms sleep — aggregated `{ sent, failed, failureDetails }` response. Never fail-fast, never bulk-throw. Resend free tier is ~2/sec, so 100ms is the natural pacing.
- Stream-template seeder uses Prisma `createMany({ skipDuplicates: true })` on unique `(userId, name)` — idempotent by construction; second call returns `skipped:9 created:0`.
- category filter on GET /api/email-templates extends the existing list endpoint via WHERE clause (no new GET route) — wizard in 04-05 calls `GET /api/email-templates?category=Stream:<x>` and consumes the first row.
- @aws-sdk/client-ses import explicitly forbidden in apollo.ts — verified zero via `grep -c "@aws-sdk/client-ses" apollo.ts == 0`. This is the firewall keeping Resend and SES code paths separate.
- Boot-time fail-fast smoke deferred to plan 04-06 deploy — executor env has no DATABASE_URL so `npm run dev` cannot reach the Resend guard locally (Prisma crashes first). The static guard code is deterministic.

## Decisions Made (Phase 04 additions — Plan 04-05)
- NetSuiteCampaignWizard send button POSTs `/api/apollo/send-campaign` (Resend), NOT `/api/campaigns/:id/send` (SES). USER-LOCKED 2026-05-30 firewall. Verified via `grep -c "campaignsApi\.send\|campaignsApi\.create\|campaignsApi\.addCompany" wizard.tsx == 0`.
- Wizard does NOT create a Campaign DB row in Phase 4 MVP — send tracking lives only in the Resend dispatcher response `{ sent, failed, failureDetails }`. Phase 4.5 or beyond may add a `Campaign` row if Rajesh requests historical reporting.
- 3-layer template fallback: `Stream:<suggestedStream>` → `Stream:Other` → `HARDCODED_FALLBACK` const. Layer 3 yields `templateId = null`, which Step 3 send button intercepts with a friendly "go seed templates first" error rather than letting the backend 404. Keeps the UX clean while preserving the backend invariant that `/api/apollo/send-campaign` requires a real persisted template.
- From-address in Step 3 preview hardcoded to literal `'Sara <sara@techcloudpro.com>'` (line 52, `APOLLO_FROM_DISPLAY`). Matches backend `apollo.ts:53 APOLLO_FROM_EMAIL`. Both stay in sync — Phase 4.5 will make per-stream / configurable in one swap.
- `campaignsApi.aiGenerateContent` IS imported and used in the wizard (Step 2 "Let AI write it for me ✨" button). That endpoint is content-generation only, NOT a send call — the firewall is on send paths, not on AI-content. Verified by counting only the send-related strings in the negative grep.
- `contactsApi.getByIds` extends the existing `GET /api/contacts` handler with a `?ids=cuid1,cuid2,...` filter (4-line WHERE addition) instead of a new endpoint — preserves auth, tenant scoping, pagination. Acceptable per the verify block's "if file is named contact.routes.ts, edit that one" guidance (production-crm uses `contacts.ts`).
- Body preview in Step 3 runs through `DOMPurify.sanitize` (mirrors `FollowUpWizard.tsx:621` pattern) — defense in depth against hostile HTML in templates or AI output.
- Doc comment containing literal `"campaignsApi.create/addCompany/send"` strings was rewritten before commit because it broke the negative grep firewall (returned 1 instead of 0). Reworded to convey the same intent without the literal anti-pattern strings.

## Decisions Made (Phase 03.1 additions — Plan 03.1-01)
- Backup branch `backup/pre-3.1-production-state` forked from `production` POST-log-commit (SHA 656afa0), not pre-log (0139337) — rollback restores log artifact AND original 23 Phase 4 commits in one shot
- Backup pushed to origin (`origin/backup/pre-3.1-production-state`) — survives any local-only disaster
- Work branch `sync/03.1-reconcile-with-ec2` forked from same SHA — Waves 2-3 start aligned with backup
- Baseline verification log force-added past .gitignore (precedent: Phase 04-01 audit SQL was also force-added)
- REQ-031D verified as a no-op: `git show origin/production:backend/prisma/schema.prisma | grep -c '^model (Quote|Contract|ContractOTP)'` returned 3 — Phase 02 models already on origin/production AND in EC2 prod DB; no preservation work needed
- Remote name correction: production-crm repo pushes to `github.com/jeet-avatar/production-crm`, NOT `github.com/jeet-avatar/crm-email-marketing-platform` (the latter is the BrandMonkz repo per MEMORY)
- No commit on work branch yet — Task 2's deliverable is the branch refs themselves; plan was designed as git-refs-only

## Decisions Made (Phase 03.1 additions — Plan 03.1-03)
- Fallback INVOKED for the EmailTemplate.category ADD COLUMN trap: prisma migrate diff emitted `ALTER TABLE "email_templates" ADD COLUMN "category" TEXT;` because origin/production schema lacks the column, but RESEARCH §2.1 verified the column ALREADY EXISTS in prod DB (predates Phase 4). Stripped the redundant block via inline Python `re.sub`, preserving `CREATE INDEX email_templates_category_idx`. Without this fix, `prisma migrate deploy` would fail with `column "category" of relation "email_templates" already exists`
- Migration directory name uses 14-digit `20260530120000` (noon UTC) per RESEARCH §4.2 — sorts last lexically, gives breathing room for same-day siblings
- tsc --noEmit DEFERRED per critical_constraints: no local node_modules, no .ts files touched in this plan (only one .sql migration), so behavioral risk = 0. Phase 4 Wave 4 CI runs npm install + tsc as the real gate
- Stray `backend/schema.prisma` orphan got mutated by `prisma format` because Prisma 5.4.2 prefers `./schema.prisma` over `./prisma/schema.prisma` when both exist in cwd. Reverted the orphan via `git checkout backend/schema.prisma`; canonical `prisma/schema.prisma` untouched throughout (verified via explicit `--schema=prisma/schema.prisma` validate)
- OQ1 (RESEARCH §9) resolved: `grep -nE '\bapolloId\b' backend/prisma/schema.prisma` returns 0 hits — no apolloId collision with Phase 4's apolloPersonId/apolloOrgId
- Cross-check against audit SQL passed: column-name counts match exactly (stream: 4=4, apolloPersonId: 3=3, apolloRawData: 2=2, apolloOrgId: 3=3)
- Force-add migration past .gitignore (`git add -f backend/prisma/migrations/.../migration.sql`) — same pattern as Plan 02 and Phase 04-01
- Single atomic commit `a5872fd` with author `jeet-avatar <jm@techcloudpro.com>` — one migration file → one commit

## Decisions Made (Phase 03.1 additions — Plan 03.1-02)
- Tarball+scp recipe (RESEARCH §5.1) chosen over per-dir scp — single network round-trip, binary stream preservation guaranteed by tar, no SSH line-ending translation risk; critical for Prisma checksum validation
- Single atomic commit for all 17 backfilled files (per RESEARCH Open Question 3) — backfill is one logical operation, not 17 independent changes
- Tarballs cleaned on both ends as final Task 1 step BEFORE Task 2 commit — verified via `ls /tmp/phase31-migrations.tar.gz` returning "No such file" on both local and EC2
- Force-add 17 migration.sql files via `git add -f` per established Phase 04-01 + Phase 03.1-01 precedent (`backend/.gitignore` line 49 blocks `prisma/migrations/**/*.sql`)
- EC2 `/var/www/crm-backend/` left untouched — only `/tmp/` written then cleaned; post-task `git status` on EC2 matches RESEARCH §1.5 baseline exactly (only pre-existing stray backend/schema.prisma + 3 .env.bak files)
- Prisma 5.4.2 fallback validate pattern: `DATABASE_URL="postgresql://dummy:dummy@localhost:5432/dummy" npx -y prisma@5.4.2 validate` — works around missing local node_modules + Prisma 7.x's breaking schema-syntax change (Plan 03.1-03 will use the same pattern)
- Pre-existing condition documented: `npx prisma validate` (without version pin) fails with Prisma 7.8.0 P1012 error regardless of backfill state — confirmed by stashing Task 1 output and re-running validate; the failure is environmental, not behavioral
- Byte-for-byte preservation verified: 5/5 spot-check sizes match RESEARCH §1.2 expected values exactly (194 / 93 / 7898 / 1414 / 316)

## Decisions Made (Phase 04 additions — Plan 04-06 PARTIAL)
- Plan 04-06 is intentionally PARTIAL. Only Task 1 (handoff wiring on ApolloPage) executed — commit `f7e6482`. Tasks 2 (deploy: rsync + pm2 + prisma migrate deploy), 3 (seed stream templates), 4 (1-real-contact Resend smoke) are PAUSED by explicit user instruction pending EC2 access + RESEND_API_KEY on EC2 + DATABASE_URL availability. Do NOT mark Plan 06 complete and do NOT write 04-06-SUMMARY.md until those tasks finish.
- Task 1 implementation: imported `NetSuiteCampaignWizard` named export, added `showWizard` state, replaced disabled placeholder button (with "Wizard handoff lands in plan 04-05" tooltip) with an enabled gradient button gated on `result?.contactIds.length > 0`, mounted wizard at top of result section with `importedContactIds=result.contactIds` + `suggestedStream=result.suggestedStream` + no-op `onSuccess` that keeps result panel visible. `tsc --noEmit` clean.
- `ContactList.tsx` remains untouched per Plan 04-04/04-05 locked decision — verified `git diff frontend/src/pages/Contacts/ContactList.tsx | wc -l == 0` post-commit.

## Decisions Made
- Prisma migration applied via `db push` (non-interactive) instead of `migrate dev` (requires TTY); manual migration SQL file created for audit trail
- Decimal fields serialized with `Number()` helper in quotes route to prevent Prisma Decimal serialization issues
- Hard delete (not soft delete) used for quotes and contracts per plan spec
- Deal ownership validated before creating quote/contract
- New Quote button gated to CLOSED_WON stage in DocumentsTab shell; Wave 3 will wire the actual handlers
- ArrowTopRightOnSquareIcon used for view-detail button on DealBoard cards (confirmed in heroicons package)
- dealsApi.getById was already present in api.ts — no addition needed
- Plan 03: Modal state (showQuoteBuilder/showContractEditor) owned inside DocumentsTab, not passed as callbacks from DealDetail — reduces coupling
- Plan 03: resolveVariables stores resolved content at submit time; raw template (with placeholders) is not persisted separately
- Plan 03: window.prompt used for SIGNED contract signer name — keeps UI simple for an edge-case interaction
- Plan 03: Subtotal/total computed inline during render (not useState + useEffect) to avoid stale state

## Decisions Made (Phase 03 additions)
- DealStage enum cast used in bulk-import — STAGE_MAP string values map exactly to Prisma enum members
- Missing FK lookups result in null (deal still imports) — avoids blocking import on unresolvable contacts/companies
- router.use(authenticate) covers bulk-import route — no per-route middleware needed

## Decisions Made (Phase 05 additions — Plan 05-03)
- `ApolloPersonalizedCampaignResponse` imported as a `type` (not value) from `../services/api` — only used in `useState` generic + variable type annotations. Matches the Wave 2 export pattern (`export interface ...`)
- Step 3 useEffect deps literal `[step, firstContactId, templateId]` with `eslint-disable-next-line react-hooks/exhaustive-deps` (suppresses fetchPreview reference warning while keeping the documented deps minimal — fetchPreview reads firstContactId+templateId+selectedIds+suggestedStream from closure but only the first two should retrigger). Closes MEDIUM #11.
- Back-button cache invalidation written as MULTI-LINE arrow body (3 statements inside `onClick={() => { ... }}`) — semantically identical to plan's pinned single-line form, but plan's verify-block literal grep returns 0 on multi-line. Behavior + tsc + runtime all unchanged. Documented in SUMMARY deviations.
- Removed dead `bodyPlain` / `validationWarnings` / `sendBlocked` derived state (17 lines) — orphaned after the Step 4 Review JSX was rewritten to use `previewResult.audit[0]` instead of `subject`/`body`. Project rule #5 honored.
- Removed `sentCount` / `failedCount` state declarations + setters — replaced by `sendResult: ApolloPersonalizedCampaignResponse | null`. Phase 4 success-counter pattern obsolete; Phase 5 carries 7-field cost telemetry envelope + per-contact audit + failure details inline.
- Step 5 (Done) "Send another campaign" button resets ALL Phase 5 state — sendResult, previewResult, previewError, sendError, confirmedLargeBatch — not just step. Prevents stale preview/cost data carrying into a fresh wizard cycle.
- `handleSend` body rewritten in Task 1's commit (not Task 2). Folding it into Task 1 avoids an intermediate revision where the commit references both `apolloApi.sendCampaign` (old, would survive on the OLD Step 3 Review block calling handleSend) AND `apolloApi.sendPersonalizedCampaign` (new fetchPreview). Negative-grep firewall on `apolloApi.sendCampaign\b` returns 0 starting with Task 1's commit `8344905`, not Task 2's. Each commit remains reviewable in isolation: Task 1 = "all state + handler logic", Task 2 = "all JSX rewrite + dead-code cleanup".
- Step 2 'Next' button label updated from 'Next: Review →' → 'Next: AI Personalize →' (kept inside Task 1's commit since it pairs with the state-machine extension).
- Step indicator container `flexWrap: 'wrap'` added — 5-label flex row with 'AI Personalize' (3 chars longer than longest existing label) would overflow the 760px max-width wizard modal on narrow viewports. Rule-2 (missing critical functionality) auto-fix for UX.
- Live send body field `confirmedLargeBatch: selectedIds.size > LARGE_BATCH_THRESHOLD ? true : undefined` — sends `undefined` (NOT `false`) for small batches so backend's `req.body.confirmedLargeBatch === true` check from Plan 05-02 SUMMARY semantics preserves the cost-gate behavior by absence-of-flag.
- `firstContactId = Array.from(selectedIds)[0] || null` derivation uses `selectedIds` (what the user CHECKED in Step 1) NOT `importedContactIds` (the superset from Apollo import). Preview operates on the same set as live send — eliminates the bug where a user unchecks contact #1 and the preview still researches them. Closes HIGH #5 (canonical-name correctness).
- `apolloApi.sendPersonalizedCampaign` reference count = 3 (1 doc-comment update on line 16 + 1 fetchPreview call + 1 handleSend call). Two real call sites, one comment-only reference. Comment updated from `apolloApi.sendCampaign` for documentation accuracy.

## Decisions Made (Phase 05 additions — Plan 05-02)
- Auth idiom: `req.user?.id || req.user?.sub` matching the adjacent /send-campaign route (NOT `req.user?.userId` as plan suggested). Same Rule-3 deviation as Plan 05-01 — local convention is `req.user.id`. Using `userId` would have 401'd every authenticated request.
- Non-object JSON FOLDED into 'Claude returned malformed JSON' branch via manual `throw new Error('non-object JSON')` inside the try/catch around `JSON.parse`. The internal Error message is never surfaced to the caller — only the warning string is. Verified by sed-scoped grep within `personalizeContactWithClaude` body returns EXACTLY 5 distinct warnings; `grep -c "'Claude returned non-object JSON'"` = 0. BLOCKER #1 closed exactly per plan.
- Imported `Prisma` namespace alongside `PrismaClient` from `@prisma/client` to enable `Prisma.InputJsonValue` cast on aiTokens write + `Prisma.JsonNull` literal for null case (replaces explicitly-forbidden `as any`).
- `tools: [{ type: 'web_search_20250305', name: 'web_search', max_uses: 3 } as any]` keeps `as any` on the tool literal. SDK 0.65.0 has `WebSearchTool20250305` type but inline-literal inference rejects without explicit cast. This is separate from the forbidden `as any` on Prisma JSON writes (which we did replace).
- Cost telemetry constant `RESEND_COST_PER_SEND = 0` named so future paid-Resend tier swap is a one-line edit. Response envelope keeps `claudeCostUSD + resendCostUSD + totalCostUSD` separate fields so consumers (wizard, dashboards) don't need to change shape when paid pricing flips.
- Curated `apolloRawData` 10-key whitelist: title, headline, seniority, departments, linkedin_url, employment_history, organization_industry, organization_size, organization_short_description, organization_founded_year. Token-cost guard: adds ~200 input tokens per call vs. 2-3k if full raw data passed.
- 250ms pacing constant (`PERSONALIZE_PACING_MS`) for new route, NOT the 100ms used by Phase 4 /send-campaign. Resend free-tier limit is 5 req/sec; 100ms = 10 req/sec which exceeds. The two routes are intentionally decoupled — Phase 4 100ms stays as-is for backward compat with its existing smoke.
- previewOnly path: `effectiveContactIds = previewOnly ? contactIds.slice(0, 1) : contactIds`. Caps at FIRST contact only. Hard cost gate (`PERSONALIZE_BATCH_HARD_CAP=50` requires `confirmedLargeBatch:true`) is SKIPPED when previewOnly (because preview is always 1 contact). Audit row written with `status='preview'` so cost is tracked even on previews.
- testRecipient opt-in only: when set (and non-empty string), `toEmail` overrides `contact.email`; value also persisted to `personalized_email_sends.testRecipient` column for permanent audit. Frontend wizard NEVER passes testRecipient — only CLI smoke tools / scripted callers.
- Local Prisma client regenerated with `DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy npx prisma generate --schema=prisma/schema.prisma` (Prisma v5.22.0) to confirm tsc passes on new code. Refresh resolved pre-existing Phase 4 errors (apolloPersonId/apolloOrgId/stream "does not exist") AND new Plan 05-01 (`personalizedEmailSend` "does not exist"). Tooling-only fix — no source change committed.
- Frontend method signature uses single params object `(params: ApolloSendPersonalizedRequest)` NOT positional args like sendCampaign(contactIds, templateId, suggestedStream). Preferred TypeScript idiom for 6-field input; matches modern apolloApi.normalize pattern. 600s (10 min) axios timeout covers 50-contact cap with margin.
- STREAM_TEMPLATE_V2_BODY used as fallback ONLY when `template.htmlContent` is empty. Primary path uses `template.htmlContent` (which Plan 05-04 deploy will populate via the /upgrade-streams-v2 endpoint built in 05-01).

## Decisions Made (Phase 05 additions — Plan 05-01)
- New `PersonalizedEmailSend` table chosen over extending `email_logs` — avoids `campaignId NOT NULL` FK that Phase 4 intentionally doesn't populate (RESEARCH §5 option c). Zero coupling with Phase 4 SES/campaigns/tracking/scheduler consumers of email_logs.
- Per-stream tailored fallback strings (not generic-only) — each of 9 streams has its own intentHook/painPoint/cta in `STREAM_FALLBACKS` dict; GENERIC_FALLBACK provides defense for unknown stream values. Wave 2 send route imports `getStreamFallbacks(stream)` when Claude returns null.
- Idempotent template upgrade via sentinel-substring detection (`htmlContent.includes('{{intentHook}}')`) — re-runs short-circuit to `alreadyV2` list, no transaction needed.
- Actual file is `backend/src/routes/emailTemplates.ts` (camelCase) NOT `email-templates.ts` (plan was incorrect; there's a dormant stub at that path too). Edited the live file confirmed via `grep -rn "seed-streams" backend/src/`.
- Auth idiom adapted to existing convention: `req.user?.id` (matches `/seed-streams` handler 5 lines above) instead of `req.user?.userId` (plan-suggested).
- Migration directory uses 14-digit `20260531120000` (noon UTC) following Plan 03.1-03 precedent.
- Force-add migration .sql past `backend/.gitignore` (line 49 ignores `prisma/migrations/**/*.sql`) — same pattern as Phase 04-01, 03.1-02, 03.1-03.
- Did NOT run `prisma migrate dev` or `prisma migrate deploy` — deferred to Plan 05-04 deploy wave. Only generated migration .sql file + validated schema via `prisma 5.4.2 validate`.
- Did NOT run `tsc --noEmit` — no local node_modules per env_state; the route file uses idioms already proven in the same file (seed-streams handler 5 lines above). TS gate falls to Plan 05-04 deploy wave.
- Schema model count 53 → 54 confirmed via baseline-vs-current grep. The 162-line schema.prisma deletion in the commit diff is `prisma format` whitespace reflow, not content removal.

## Phase 05 Progress
| Plan | Name | Status | Commit |
|------|------|--------|--------|
| 01 | Prisma migration + 9 stream templates upgraded to v2 with AI placeholders + idempotent upgrade endpoint | Complete | b19feec, 820fd4a |
| 02 | Backend personalizeContactWithClaude + POST /api/apollo/send-personalized-campaign + frontend client | Complete | 908e631, 35bbc58 |
| 03 | NetSuiteCampaignWizard 4→5 step refactor with AI Personalize Step 3 + Review Step 4 + Done Step 5 | Complete | 8344905, 46151a4 |
| 04 | Deploy + Prisma migrate + template upgrade curl + 4-case smoke + close ceremony | Complete | b19feec..2e1f402 (9 pushed) + close commit |

**Phase 05 COMPLETE 2026-05-31** — 4/4 plans done. End-to-end live: Apollo contact (Ricardo Deben / Centella Health Tech) → Claude web_search → 4 personalized tokens → Resend dispatch from Sara to jm@techcloudpro.com. 2 emails delivered (msgIds e95edd09 + 1ab6897c). Total ~$0.374 spend. Phase 4 firewall: 0-line diff vs `phase-04-baseline` tag.

## Decisions Made (Phase 05 additions — Plan 05-04)
- Stable firewall anchor tag `phase-04-baseline` at commit 0175cc3 (last pre-Phase-5 commit on campaigns.ts) — replaces fragile `git log --grep` heuristics with reproducible tag-based diff (MEDIUM #12 closed)
- Anthropic pre-flight probe BEFORE deploy/smoke — claude-sonnet-4-6 + 10-token ping returned type=message, confirmed workspace key + model access; ~$0.000029 (MEDIUM #9 closed)
- Authoritative migration verification via psql `_prisma_migrations.finished_at IS NOT NULL` for migration_name LIKE '%phase05_personalized_email_send%' — survives the 2 pre-existing failed migrations' P3018 noise that taint the `prisma migrate deploy` exit code (HIGH #6 closed)
- Authoritative template-upgrade verification via psql LIKE '%{{intentHook}}%' on smoke template's htmlContent — guarantees the upgrade endpoint hit the right row, not a silent no-op (HIGH #7 closed)
- TIGHT Case A acceptance: personalized=1 AND personalizeFailures=0 AND audit[0].aiTokens.intentHook non-null AND audit[0].renderedBody does NOT contain literal {{intentHook}} AND audit[0].renderedBody contains "Ricardo" — AI claim falsifiable (a fallback path FAILS this case) AND falsified (i.e., proven, on prod EC2 against Ricardo Deben) (HIGH #3 closed)
- Implicit checkpoint approval honored: user progressed to next request (branded TCP v6 template integration) after Cases B + C delivered 2 emails to JM inbox — counted as APPROVED per execute-plan checkpoint protocol
- Frontend ~5s broken-window during Vite assets eviction acceptable for brandmonkz (low-traffic CRM admin); brandmonkz-NEXT atomic-swap pattern deferred to future phase (MEDIUM #10 documented)
- 9 commits pushed b19feec..2e1f402 fast-forward to origin/production with no --force; branch at parity post-push (0 commits ahead)
- Branded TCP v6 template integration LOGGED as deferred — Sara already correct sender per memory `tcp_v6_sender_rotated` (rotated 2026-05-26); integration is template-substitution exercise (STREAM_TEMPLATE_V2_BODY → v6 shell with same 4 placeholders); separate work item, NOT a Phase 5 gap

## Accumulated Context

### Roadmap Evolution
- Phase 03.1 inserted after Phase 03: Repo + Schema Reconciliation (URGENT) — blocks Phase 04 Wave 4 (deploy + smoke). Local schema.prisma is 32 models / 1399 lines; prod is 50 models / 2184 lines. 18 prod-only models (ApiKey, AuditLog, UserSession, MediaAccess, PartnerProgram, EmailUnsubscribe, etc.) would be silently dropped by current Phase 04 deploy plan. 3 local-only models (Quote, Contract, ContractOTP from Phase 02). 14 migrations in prod _prisma_migrations table not in local backend/prisma/migrations/. Local clone is 23 commits ahead of origin/production.

## Blockers/Concerns
- **Upcoming work item (user-flagged, NOT a Phase 5 blocker):** branded TCP v6 template integration — Phase 5 currently ships AI-personalized content inside the plain v2 stream template body. User wants the 4 AI tokens (intentHook/companyContext/painPoint/cta) wrapped in the v6 branded HTML shell that Sara's existing TCP retargeting pipeline already uses (per memory `tcp_v6_sender_rotated`). Sara is already the correct sender — integration is a template-substitution exercise (STREAM_TEMPLATE_V2_BODY → v6 shell with the same 4 placeholders). Separate work item.
- Phase 4.5 reopen trigger: fresh Apollo API key needed on EC2 (`/var/www/crm-backend/.env` has 2 duplicate APOLLO_API_KEY entries, both stale, both upstream-401). See `.planning/phases/04-apollo-import-and-auto-campaign/deferred-items.md` item #1. Resolution effort: <5 min once key is in hand.
- 2 pre-existing failed migrations on prod DB (`20251112_add_isActive_to_video_template`, `20251112_fix_campaign_timestamps`) emit P3018 on every `prisma migrate deploy` run. Phase 4 + Phase 5 migrations ran cleanly after them; they are prior-phase debt. Cleanup is a separate quick task.

## Phase 03.1 Progress
| Plan | Name | Status | Commit |
|------|------|--------|--------|
| 01 | Backup branch + work branch + baseline verification (REQ-031A precondition + REQ-031D no-op confirmation) | Complete | 656afa0 |
| 02 | Backfill 17 EC2-only migration directories via tarball+scp (REQ-031B) | Complete | 24a5c66 |
| 03 | Generate Phase 4 migration via `prisma migrate diff` + REQ-031F gates (REQ-031C, REQ-031F) | Complete | a5872fd |
| 04 | Fast-forward push to origin/production + ROADMAP/STATE update (REQ-031A, REQ-031D, REQ-031E, REQ-031F) | Complete | 0745cc0 (push) + a12ff42 (doc) |

**REQ-031D status:** NO-OP — Quote/Contract/ContractOTP models verified already on origin/production schema AND in prod DB `_prisma_migrations` table (migration `20260319000000_add_quotes_contracts` applied). Phase 02 preservation step from the original 03.1 scope is not needed.

## Phase 04 Progress
| Plan | Name | Status | Commit |
|------|------|--------|--------|
| 01 | Prisma schema (Contact/Company stream + Apollo IDs) + classifyStream extraction | Complete | 911e1e2, 79290fe |
| 02 | Apollo TS client lib (searchPeople + enrichPerson + typed errors) | Complete | 2113f9e, 5c75d21 |
| 03 | Backend POST /api/apollo/import + /api/apollo/send-campaign (Resend) | Complete | 764ce91, 9437281, 243354b |
| 04 | Dedicated /apollo page + ApolloSearchForm + sidebar nav | Complete | 66a4737, b005169 |
| 05 | NetSuiteCampaignWizard component (Resend send + 3-layer template fallback) | Complete | 9371f7c, 2f14467 |
| 06 | Handoff wiring + deploy + seed + smoke | Complete (Resend send 200; Apollo import 503 → upstream-credential gate; htmlContent fix landed) | f7e6482, de87ba1, f44e38a, a72fa4b |

## Decisions Made (Phase 04 additions — Plan 04-06 final)
- Phase 4 closed despite Apollo IMPORT 401 from upstream: the wrapper's graceful 503 path is itself verified, the only remaining gate is a credential refresh outside this repo, and Resend dispatch (the new code the phase shipped) is fully proven. Filed as Phase 4.5 reopen trigger in deferred-items.md item #1.
- apollo.ts:289 read `(template as any).htmlBody || (template as any).body || ''` — both fields are nonexistent on the post-Phase-03.1 EmailTemplate schema. The real column is `htmlContent`. Plan 04-03 used `as any` casts that masked the type error. Fixed to `template.htmlContent || ''` in commit a72fa4b. Production-blocking bug; sends silently 400'd until patched. User verified inbox receipt post-fix.
- JWT minted out-of-band must include `issuer:'crm-api'` + `audience:'crm-client'` claims (AuthUtils.verifyToken enforces both). First mint without them returned 401 on /contacts; pattern documented for future ops use.
- Send-campaign smoke uses the existing test contact `cmtest1780181866jm6a063b2d` (jm@techcloudpro.com) per user-locked instruction — NOT a real Apollo-imported prospect. Resend dispatcher verification does not require Apollo to succeed first.
- Test contact LEFT IN PLACE post-smoke — keeps the smoke loop trivially re-runnable. Filed in deferred-items.md item #2.
- nginx 403's the default `curl/8.x` UA. All curl-to-prod must set `User-Agent: Mozilla/5.0 (X11; Linux) BrandMonkz-Smoke/1.0` or equivalent. Lesson learned; will apply to all future EC2 smoke testing.

## Phase 03 Progress
| Plan | Name | Status | Commit |
|------|------|--------|--------|
| 01 | Backend POST /api/deals/bulk-import | Complete | e865340 |
| 02 | MigrationWizardModal full 5-step wizard | Complete | 300d81e |
| 03 | SettingsPage Data Import tab + MigrationWizardModal wired | Complete | 85760bf |

## Phase 02 Progress
| Plan | Name | Status | Commit |
|------|------|--------|--------|
| 01 | Quote + Contract data layer (Prisma + routes) | Complete | c70ad18 |
| 02 | DealDetail page + Documents tab shell + route wiring | Complete | 8776be9 |
| 03 | QuoteBuilder modal + ContractEditor modal + DocumentsTab fully wired | Complete | 683846e |

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 1 | Fix Indigo Noir dark theme: remap orange/rose CSS to indigo/purple, fix missing gray text classes, fix light badge backgrounds | 2026-03-17 | f3301d7 | Verified | [1-fix-indigo-noir-dark-theme-remap-orange-](.planning/quick/1-fix-indigo-noir-dark-theme-remap-orange-/) |
| 2 | Build Job Leads Pipeline: Remotive API fetch + 4-stream classification + /job-leads page + Company+Contact import | 2026-03-26 | 9a23c86, d32ec65 | Deployed | [2-build-job-leads-pipeline-for-brandmonkz-](.planning/quick/2-build-job-leads-pipeline-for-brandmonkz-/) |
| 3 | Enhance Job Leads: domain emails (hr@), hero CTA + pulse animation, dismissible guide panel, email pill column, email on Contact import | 2026-03-26 | 10a7f0a, 667df4f, fda181e | Deployed | [3-enhance-job-leads-page-real-company-emai](.planning/quick/3-enhance-job-leads-page-real-company-emai/) |
| 5 | Wire ContactList ?source=apollo filter end-to-end: backend where.source + frontend API signature + URL-driven sourceFilter state + dismissible chip + Apollo/stream badges. Live-verified: GET /api/contacts?source=apollo returns exactly 1 row (cmpsz0d3q000350mxrlau3sg5 / Ricardo Deben / Cybersecurity) with zero non-apollo leak | 2026-05-30 | 8b99d33 | Deployed | [5-wire-contactlist-source-filter-apollo-st](.planning/quick/5-wire-contactlist-source-filter-apollo-st/) |
| 6 | Apollo 120s per-call axios timeout (apolloApi.import + sendCampaign only) + backend 3-case Company find-or-update-or-create ladder eliminating P2002 companies_domain_key crash. Live-verified: 2 consecutive `POST /api/apollo/import` runs both HTTP 200 in ~9s, 4 imported + 1 skipped per run, zero new P2002 in pm2 logs. State correction confirmed Apollo key is LIVE (not 503). | 2026-05-30 | 861ed00 | Deployed | [6-fix-apollo-bugs-120s-per-route-timeout-c](.planning/quick/6-fix-apollo-bugs-120s-per-route-timeout-c/) |
| 7 | Apollo Claude auto-normalize on Search + form UX upgrades: claude-sonnet-4-6 normalize-filters endpoint + autoNormalize default-true into /import + new ICP presets + per-field ✓/✗ hints + "🤖 Claude refined your filters" results block. Rule-1 deviation: bumped timeout 3s→8s (claude-sonnet-4-6 needs 3.3-5s for full system prompt). Live-verified 3/3 cases: Case A dirty `/normalize-filters` → 200 + 1 correction (Saas Companies in Irvine → SaaS tag + Irvine location); Case B dirty `/import` → 200 + 3 imported + 1 correction; Case C clean → 200 + 0 corrections (no false positives, 2 imported). | 2026-05-30 | d0a3708, 17c2ceb | Deployed | [7-apollo-form-claude-auto-correct-on-searc](.planning/quick/7-apollo-form-claude-auto-correct-on-searc/) |
